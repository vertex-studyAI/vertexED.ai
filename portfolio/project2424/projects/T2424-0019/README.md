# T2424-0019 — NPMS — Neural Predictive Memory Spectroscopy

This is the canonical Project 2424 recovery package for First-100 rank #3. The frozen queue title above is authoritative; the retained provisional source identity `MODEL-NPMS` is preserved as recovery provenance rather than treated as a competing project identity.

NPMS is preserved as a **diagnostic system for predictive memory**, not promoted into a generic forecasting architecture. The retained isolated package analysed fitted latent dynamics through eigenmodes, timescales, frequencies, singular spectra, impulse/resolvent responses and modal interventions. All executed evidence in the recovered bundle is `SYNTHETIC_CONTROLLED`.

## What was actually recovered

The canonical evidence package retains the isolated compact report and validates its claim boundary. It does not reconstruct missing source from prose and does not claim the original implementation has been rerun from this Git repository.

## Required weaknesses remain explicit

- matched-eigenvalue error can omit missing/spurious modes;
- mode truncation can mishandle conjugate pairs;
- frequency-response analysis is a resolvent proxy rather than a complete identified transfer function;
- delay-PCA recovery weaknesses remain;
- multiscale limitations remain;
- switching-system limitations remain.

These are scientific limitations to preserve and test, not wording defects to erase.

## Promotion boundary

`MODEL-NPMS` source/config/result migration, clean reproduction, improved spectral metrics, conjugate-pair-aware truncation, switching-system stress tests, delay-embedding ablation and independent QA remain open gates.

Until those are satisfied, this package remains an evidence-boundary recovery and is **not `RESEARCH_COMPLETE` or `CERTIFIED_COMPLETE`**.
