# Benchmark Augmentation Theory — Auxiliary Status

**Identity:** AUX-P2424-BENCHMARK-AUGMENTATION  
**Project:** Benchmark Augmentation Theory  
**State:** PRESERVED_AUXILIARY_TOOL / LATEST_MAIN_REVALIDATION_PENDING  

## Why this identity exists

The implementation previously occupied `T2424-0050`, but the frozen First-100 queue assigns that ID to **Darcy Latent Operator**. This relocation preserves the benchmark-audit artifact without allowing it to impersonate or inflate the canonical First-100 entry.

## Implemented

- deterministic shortcut benchmark;
- causal-signal and shortcut predictors;
- label-preserving augmentation engine;
- label-change fail-closed check;
- shortcut-breaking perturbation;
- neutral signal-scaling control;
- base/augmented ranking comparison;
- runnable deterministic screen;
- focused regression suite retained at repository root.

## Claim boundary

Synthetic ranking-audit mechanism only. No benchmark-validity theorem, semantic guarantee for arbitrary perturbations, real-model robustness, publication novelty, or research-complete claim.

## Current gate

This auxiliary package is being replayed on current `main` only to preserve the useful prior artifact while restoring `T2424-0050` to Darcy. Canonical CI must pass on the fresh repair head before the relocation is eligible for manual integration.

This auxiliary package must never be counted as a second First-100 project.
