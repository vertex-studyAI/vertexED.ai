# Benchmark Augmentation Theory — Auxiliary Status

**Identity:** AUX-P2424-BENCHMARK-AUGMENTATION  
**Project:** Benchmark Augmentation Theory  
**State:** PRESERVED_AUXILIARY_TOOL / CURRENT_MAIN_RELOCATION_CI_PENDING  

## Why this identity exists

The implementation was previously merged under `T2424-0050`, but the frozen First-100 queue assigns that ID to **Darcy Latent Operator**. This relocation preserves the tested benchmark-audit artifact without allowing it to impersonate or inflate the canonical First-100 entry.

## Implemented

- deterministic shortcut benchmark;
- causal-signal and shortcut predictors;
- label-preserving augmentation engine;
- label-change fail-closed check;
- shortcut-breaking perturbation;
- neutral signal-scaling control;
- base/augmented ranking comparison;
- runnable deterministic screen;
- focused regression suite retained at repository root.

## Claim boundary

Synthetic ranking-audit mechanism only. No benchmark-validity theorem, semantic guarantee for arbitrary perturbations, real-model robustness, publication novelty, or research-complete claim.

## Current gate

Canonical repository CI must pass on this fresh current-main relocation branch. The canonical `T2424-0050` namespace is reserved for Darcy Latent Operator.
