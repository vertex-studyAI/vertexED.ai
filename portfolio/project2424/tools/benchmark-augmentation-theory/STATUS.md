# Benchmark Augmentation Theory — Status

**Identity:** `AUX-P2424-BENCHMARK-AUGMENTATION-THEORY`  
**Registry status:** auxiliary tool; not a First-100 entry  
**State:** MIGRATED_AWAITING_CURRENT_HEAD_CI  

## Preserved functionality

- deterministic shortcut benchmark
- causal-signal and shortcut models
- label-preserving augmentation engine
- fail-closed label-change rejection
- shortcut-breaking perturbation
- neutral augmentation control
- ranking comparison
- runnable screen
- focused regression suite

## Historical evidence

The same implementation was exact-head tested before its prior merge. This relocation must pass canonical CI again before the auxiliary path is treated as current-head verified.

## Identity correction

The frozen First-100 queue assigns `T2424-0050` to **Darcy Latent Operator**. This benchmark-audit tool therefore must not contribute a First-100 count under `T2424-0050`.

## Claim boundary

Synthetic benchmark-ranking audit mechanics only; no real-model robustness, benchmark-validity theorem, publication novelty, or research-complete claim.
