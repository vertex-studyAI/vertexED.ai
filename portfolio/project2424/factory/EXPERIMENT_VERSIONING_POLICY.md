# Experiment Versioning Policy

Frozen outcomes are immutable.

If a protocol, dataset, baseline, threshold, metric, model, seed policy, or analysis rule changes after observation, create a new experiment/protocol version. Do not overwrite the original result or silently migrate the old verdict.

Every manuscript must identify which experiment version supports each headline claim.