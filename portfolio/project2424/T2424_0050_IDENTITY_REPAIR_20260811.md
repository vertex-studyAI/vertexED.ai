# T2424-0050 — Registry Identity Repair

**Date:** 2026-08-11  
**Frozen registry identity:** `T2424-0050` — **Darcy Latent Operator**  
**Collision merged on prior main:** Benchmark Augmentation Theory

## What this repair does

1. preserves Benchmark Augmentation Theory under `portfolio/project2424/tools/benchmark-augmentation-theory/` with auxiliary identity `AUX-P2424-BENCHMARK-AUGMENTATION-THEORY`;
2. moves its existing regression import to that auxiliary implementation;
3. restores `portfolio/project2424/projects/T2424-0050/` to Darcy Latent Operator;
4. restores the already exact-head-tested Darcy source, experiment, retained result and regression suite from PR #204;
5. requires fresh canonical CI on the current-main recovery head before merge.

## Evidence inheritance boundary

Prior Darcy head `ce9331c8be3e66eb8249121aa2243c03550108d2` passed canonical CI run `31449982073`. That proves the recovered Darcy implementation was tested in its prior branch context. It does **not** substitute for fresh integration verification after relocating the benchmark-audit artifact and applying the repair to current `main`.

## Counting rule

Before this repair merges, current `main` must not count its `T2424-0050` folder as the frozen First-100 Darcy project because the folder contains the wrong identity. After this repair, `T2424-0050` may be counted as registry-aligned/tested only if fresh exact-head canonical CI succeeds.

The auxiliary benchmark-audit artifact does not count as a First-100 registry entry.

## Scientific boundary

The Darcy package is a deterministic 1D reduced-resistance mechanism screen. It is not a learned neural operator, multidimensional porous-media result, real-data validation, publication result, or research-complete project.
