# APEN / PEN Results

**Audited revision:** `0d2a14e559b0caa9b5b1cbeef0995013594ecf15`  
**Wave date:** 2026-08-12  
**Result status:** **NO REPRODUCIBLE EXPERIMENT LOCATED**

The connected portfolio contains APEN/PEN references and consolidation proposals, but this wave did not locate a standalone canonical implementation with an addressable frozen hypothesis, dataset/task, baseline, metric, seed policy, training/evaluation command, and retained machine-readable result.

Accordingly, no APEN/PEN performance result is reported. A name or architecture sketch is not experimental evidence.

## Required gate

Before APEN/PEN enters the experiment queue, freeze:

- one precise hypothesis and falsification criterion;
- canonical source path/repository and commit;
- dataset/task and immutable split identifiers;
- simple baseline and standard architecture baseline;
- proposed method and mechanism-removing ablation;
- metric implementation;
- paired seed list and failure policy;
- matched compute/training budget;
- one-command train/evaluate procedure;
- raw per-seed machine-readable metrics.

If PEN and APEN are variants of the same mechanism, compare them under one matched protocol rather than treating naming differences as independent contributions.
