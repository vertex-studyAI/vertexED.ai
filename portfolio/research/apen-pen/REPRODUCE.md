# Reproduce APEN / PEN

No valid reproduction command is available at the audited connected revision. Do not fabricate one from portfolio naming.

A future reproduction must begin by identifying the canonical implementation and frozen source commit, then record the exact environment and command before executing. The first accepted protocol must include a simple baseline, a standard architecture baseline, the proposed APEN/PEN variant, at least one mechanism-removing ablation, paired seeds, matched training budget, retained logs/checkpoints, and raw per-seed metrics.

If a bug is discovered after observing the result, preserve the original invalid run and rerun only after committing the smallest documented fix.
