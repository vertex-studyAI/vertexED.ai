# Olympus + Hercules Run-Ready Truth — 25 August 2026

## Core identity decision

The retained evidence contains two historically incompatible uses of “Olympus.” They must remain separated:

1. **Olympus runtime/research architecture** — deterministic, typed research-runtime components with actual bounded tests/evidence.
2. **Olympus scale-model roadmap** — Hermes/Prometheus/Perseus/Atlas/Kronos as proposed ever-larger trained models. This scale roadmap has no corresponding verified frontier checkpoints, training logs, data lineage, compute accounting or learned-model evaluation and remains frozen concept/roadmap material.

Trainable Transformer/local-model architecture work belongs under **Hercules** or a separate named model line, not the Olympus runtime.

## Recovered functional runtime map

| Module | Recovered functional role | Runtime implementation evidence | Trained-model claim |
|---|---|---|---|
| Prometheus | structured hypothesis generation / routing | yes, bounded runtime | none |
| Perseus | adversarial falsification / sparse routing | yes, bounded runtime | none |
| Atlas | causal/counterfactual world state | yes, bounded runtime | none |
| Hermes | verified tool and memory execution | yes, bounded runtime | none |
| Kronos | persistent research-program policy/dispatch boundary | yes, bounded runtime | none |
| Aion | branching/state support where implemented | partial/recovered runtime family | none |
| Themis | policy/contract checking | retained validated-prototype evidence | none |
| Pantheon | evidence/provenance support where implemented | partial/recovered runtime family | none |
| Mnemosyne | provenance/memory support in recovered architecture map | not independently promoted here | none |
| Percy | operational durable execution/task orchestration | separate system; not an Olympus neural module | none |

## O0 / Compact Olympus evidence

Retained evidence classifies `OLYMPUS-COMPACT` as clean-room verified / E4 test-verified with baseline + ablation executed and CPU synthetic smoke. The retained catalog revision is `9b39dea6226a0239df8140cc758dfd3b271262d0`.

A retained Typhon status record gives:

- archive SHA-256: `16ddf0261f8b19d40f17b2199741c49f3d55e78b66811c9dcfe9e38cd9c42ace`
- 12 tests
- compact Olympus accuracy: `0.6417824074`
- baseline accuracy: `0.4178240741`
- ablation accuracy: `0.5734953704`
- fp32 state bytes: `93349`
- int8 state bytes: `58933`

These are **synthetic compact-reference metrics**, not evidence of a trained frontier model.

A later isolated O0 package also records 6/6 tests covering Atlas counterfactual behavior, Hermes memory/tool behavior, Kronos scheduler-boundary behavior, permission-denial tracing, Perseus sparse top-k behavior and Prometheus routing.

## O1 — next scientifically useful experiment

**Status:** `FROZEN_SPEC / NOT_EXECUTED`.

Question: does explicit decomposition into hypothesis generation, adversarial falsification and verified tool execution improve reliable research-task completion relative to a monolithic agent under the same base provider/model, tools and task budget?

Frozen arms:

1. monolithic agent;
2. Prometheus → Perseus → Hermes;
3. decomposition minus Perseus;
4. decomposition minus evidence/policy gate.

Required design before any outcome:

- at least ~100 paired tasks across >=2 task families;
- one frozen provider/model;
- identical tool access and task budgets;
- task instances frozen before results;
- success/quality rubric frozen before results;
- task success;
- schema validity;
- unsupported-claim rate;
- falsification catch rate;
- tool correctness;
- evidence completeness;
- latency;
- token use;
- peak RAM;
- paired uncertainty/statistics;
- explicit promotion and failure thresholds.

Under the current portfolio-wide `major_scientific_outcome_runs_authorized=0`, O1 may be implemented and smoke-tested but not executed as a new scientific outcome run.

## Named component status

The strongest retained evidence says the runtime implementations of Hermes, Prometheus, Perseus, Atlas and Kronos exist/test at the bounded runtime layer. Another later portfolio matrix grades individual components unevenly—Hermes/Atlas/Kronos/Themis as bounded validated prototypes and Prometheus/Perseus as less independently mature. The correct interpretation is therefore:

- **family runtime behavior:** recovered/tested at O0;
- **independent component-level scientific mechanism evidence:** heterogeneous and incomplete;
- **trained model identity:** absent;
- **released weights:** absent;
- **parameter-scale claims:** roadmap only.

Do not upgrade a component from runtime behavior to learned-model evidence without a separate experiment.

## Hercules

**Status:** `BLOCKED_SOURCE / CONCEPT_ONLY` in the strongest retained audit.

No canonical implementation/package/experiment was recovered in the connected evidence boundary. Therefore do not build a matched Transformer comparison around an undefined mechanism.

Pre-compute advancement gate:

1. one bounded capability contract;
2. canonical source path/archive + immutable revision/hash;
3. exact proposed mechanism definition;
4. matched Transformer/reference baseline;
5. ablated proposed mechanism;
6. frozen data/budget/seed/evaluation contract;
7. smoke path;
8. machine-generated result schema/reporting;
9. explicit failure criterion.

## Morning executor target

### Olympus

`O0_FROZEN_VALIDATED_PROTOTYPE / O1_PROTOCOL_FREEZE`

Recover/hash-bind the exact O0 archive if available, verify its tests without changing mechanism, and materialize the O1 protocol/config/task-schema without running the outcome.

### Hercules

`SOURCE_RECOVERY / PROTOCOL_BLOCKED`

Recover canonical source/mechanism or stay blocked. Do not invent a new architecture and call it recovered Hercules.

## Claim boundary

Supported: a compact deterministic Olympus reference/runtime exists in retained evidence and bounded tests exercise several named runtime roles.

Not supported: frontier-scale Olympus training, parameter-scale Hermes/Prometheus/Perseus/Atlas/Kronos checkpoints, learned-model superiority, general research autonomy, or released weights.