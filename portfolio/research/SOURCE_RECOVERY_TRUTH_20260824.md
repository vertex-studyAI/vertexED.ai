# Source-Recovery Truth — 24 August 2026

**Scope:** connected GitHub repository search plus canonical control-repository evidence available during the run-ready freeze. This document is deliberately narrower than a claim that no source exists anywhere: retained local/Library archives or disconnected repositories may still contain canonical material.

## Rule

A registered research name, roadmap item, historical checkpoint, or research-stack reference is not sufficient to label a line implemented. Until an executable source tree/archive and lineage are recovered, the state remains `SOURCE_RECOVERY` or `UNKNOWN`.

## Weather-JEPA

**Connected GitHub search result:** no standalone executable Weather-JEPA project path was recovered. Hits in the control repository are research-stack/registry/planning references such as Bu1LD research-stack ATLAS/CRUCIBLE/LATTICE material and portfolio ledgers.

**Current classification:** `SOURCE_RECOVERY / UNKNOWN_IMPLEMENTATION`.

**Do next:**

1. inspect retained Library/local research archives for exact Weather-JEPA source package names and hashes;
2. if a candidate is found, establish source lineage before modifying it;
3. only then add dataset-loader identity, temporal split manifest, persistence/statistical baselines, smoke path and result schema;
4. if nothing canonical is recovered, retain UNKNOWN rather than generate a replacement and call it the old project.

## Space-JEPA

**Connected GitHub search result:** hits are limited to research/portfolio planning-status documents; no canonical executable source path was recovered from the connected control repository.

**Current classification:** `SOURCE_RECOVERY / UNKNOWN_IMPLEMENTATION`.

**Do next:** recover exact source/archive first. Only after lineage closes should a mission-data loader, chronological/anomaly split semantics, persistence/ridge/reconstruction baselines, smoke path and reporting schema be attached.

## Audio-Light / Audio-JEPA incubation

The canonical research ledger describes this as unrecovered/protocol-stage rather than a verified implementation.

**Current classification:** `SOURCE_RECOVERY / UNKNOWN`.

Do not synthesize a new implementation merely to make the registry look complete. Recover source or keep UNKNOWN.

## PEN

Canonical research evidence establishes that an independent `MODEL-PEN` evidence package exists and carries a negative compact result, but the executable source tree/archive has not been recovered into the current canonical runtime/repository evidence.

A connected installed-repository search for `MODEL-PEN` did not recover a standalone GitHub repository during this freeze.

**Current classification:** `EVIDENCE_PACKAGE_DISCOVERED / SOURCE_RECOVERY`.

**Do next:** recover the exact package from retained Library/local material, hash it, verify its manifest/workspace ledger, recover original tests/compact commands, then perform a fresh reproduction only if the source identity is proven. Never substitute APEN source/evidence.

## Eigen-Finance

Exact connected code search for `eigen_finance` resolves only to the canonical portfolio registry; broader `Eigen-Finance` hits are portfolio/checkpoint/queue references rather than an executable source tree. An older canonical checkpoint already classified it `CANONICAL_SOURCE_UNVERIFIED` and required leakage-safe temporal splits plus turnover/cost constraints if a quant protocol is recovered.

**Current classification:** `SOURCE_RECOVERY / DISTINCT_CONTRIBUTION_UNRESOLVED`.

**Do next:** search retained archives/worktrees for source and a research-question/claim record. Establish what is distinct from Eigen-JEPA and adjacent finance-representation work before any novelty or benchmark claim.

## Weather/Space source naming warning

Do not infer source identity from the names alone. “Weather-JEPA” and “Space-JEPA” may have appeared in planning documents before a standalone package existed, or may live under a different historical package/repository name. Recovery should be hash/manifest/path based, not title similarity based.

## Olympus / Hercules family

Canonical family aliases currently include:

`Olympus, Hermes, Prometheus, Perseus, Atlas, Kronos, Aion, Themis, Pantheon, Mnemosyne`.

Combined code search in the connected control repo resolves the family through the portfolio registry rather than independent executable sources for all aliases, and installed-repository search did not recover a standalone repository named Olympus during this freeze.

This does not prove that no local implementation exists; it proves that the connected evidence does not justify treating every alias as an implemented model.

**Current classification:** `ARCHIVED_COMPUTE / SOURCE_RECOVERY`.

**Do next:** build a component truth table with, for each alias: exact source path/archive/repository, hash/SHA, runnable entry point, tests, prior evidence, and whether it is implementation vs spec/name only. Significant training stays off until one matched-provider/task/tool-budget protocol is frozen.

## Hercules

The older checkpoint likewise states that no canonical standalone source plus independently reproduced bounded capability gate was verified in connected evidence.

**Current classification:** `SOURCE_RECOVERY / ARCHIVED_COMPUTE`.

**Do next:** locate canonical source or explicitly record none; then freeze one matched capability/baseline/metric/budget gate before deciding keep/archive.

## Recovery evidence standard

A recovered line should not leave `SOURCE_RECOVERY` until all applicable items exist:

- exact path/repository/archive identity;
- immutable commit SHA or archive SHA-256;
- manifest integrity where available;
- original or authoritative research question/protocol linkage;
- runnable entry point or explicit reason no runner exists;
- dependency/environment identity;
- existing raw result/evidence linkage kept separate from new runs;
- collision check against similarly named portfolio lines;
- one handoff record stating what may and may not be claimed.

## Current conclusion

The correct overnight work for these lines is **recovery**, not invention. If the canonical source is found, it can enter the run-ready pipeline. If it is not found, `UNKNOWN/SOURCE_RECOVERY` is a successful truthful outcome for the morning handoff.