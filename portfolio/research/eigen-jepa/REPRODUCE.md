# Reproduce Eigen-JEPA

No valid Eigen-JEPA reproduction command was located in the connected source audited on 2026-08-12.

Do not infer an implementation from portfolio references. First identify the canonical repository/path and freeze its source commit. Then predeclare the hypothesis, spectral/eigen mechanism, dataset/task, simple baseline, standard JEPA/representation baseline, eigen-mechanism ablation, metric, seed list, matched parameter/compute budget, and falsification criterion.

The first accepted run must retain command, commit, environment, seeds, runtime, logs, checkpoints/artifacts, raw per-seed metrics, aggregate mean/sample SD/n, paired deltas, and all failed runs. Any post-result bug fix must be versioned and reported separately from the invalid pre-fix result.
