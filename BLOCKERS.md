# BLOCKERS

**As of:** 2026-08-14 22:02 IST, reconciled against fresh connected GitHub evidence. Only blockers that prevent a stronger evidence claim are listed. `MASTER_STATUS.md` remains the canonical portfolio state and `NEXT_TASK_QUEUE.md` remains the canonical dependency order.

## P0 — Percy authoritative live state
Existing Mac SQLite/WAL/process state is not visible from this execution surface. Run `PERCY-STATE-001` non-destructively: snapshot/hash, integrity/schema, leases/heartbeats/stale reconciliation, independent recount and dirty-worktree capture. Do not reset, replace or clean the existing database to manufacture a consistent state.

The fixed registry is exactly **16,256 logical scheduling identities, `P00000`–`P16255`**. This does not establish physical workers, dispatch count, completion count, failure count, blockage count or queue depth; all such live counters remain `UNKNOWN` until the trusted host is recovered.

## P0 — VertexED exact production revision and authenticated certification
Repository `main` at checkpoint branch point is `d5e9fcaa8de4e49b236b18ff7d3c515ed5f1ed6d`. Both configured Vercel commit-status contexts on that revision report `FAILURE` with `Deployment rate limited — retry in 24 hours.` The retained production monitor evidence also fails exact served-revision identity even where bounded public/auth smoke checks pass.

Exact served revision/deployment ID and the disposable authenticated golden journey remain unproved. Do not spend money to bypass deployment limits, weaken revision assertions, or substitute source/browser-local checks for production identity.

## P0 — Project 2424 local overlay / canonical identity reconciliation
The preserved historical source anchor `bd2a4d3d939b8ce06908d7842ca9e075e0ae2fa7` is **GitHub-resolvable**, and its `portfolio/project2424` tree is readable. Therefore the blocker is no longer “no canonical source exists.”

Still missing from this execution surface: the preserved local dirty overlay/worktree state, host-only additions, dirty manifest/hashes, and any evidence needed to claim latest-byte identity. Workflow `31618609967` and artifact `9162627168` remain preserved scientific-value/evidence lineage only. Recover the local overlay from the trusted host when available, then reconcile all 2,424 identity contracts against source-backed children without treating registry count as scientific completion.

## P0 — LAM-JEPA owner-controlled release metadata
The internal scientific result, retained numerical provenance, deterministic paper assets and immutable external-review packet are already closed at the current evidence boundary. Remaining owner-only release metadata must not be inferred from commit history or old drafts: approved author list/order, license/compatibility decision, redistribution boundary, `CITATION.cff` and immutable release revision.

External validation is a separate P1 gate and remains pending until genuinely independent evidence returns. ARC superiority, planner contribution and target contribution remain unsupported, and the locked ARC test stays closed.

## P1 — LAM-JEPA independent external reproduction/review
The external packet is ready, but packet readiness is not validation. A genuinely independent validator must receive the exact immutable package and return a retained reproduction/review record including identity/date, artifact revision, observed hashes/numbers, discrepancies, source-method critique and success/failure interpretation.

## P1 — IRIS baseline-frontier source recovery
Current successor search is closed. A new architecture is not a blocker because it is not authorized. The eligible next scientific gate is the frozen development-only robust detector+estimator frontier. Exact retained development trajectories, implementations/parameters and metric code must be recovered first; confirmatory seeds remain quarantined and untouched.

## P1 — NeuroCAD broader product/science boundary
The old typed-parser causal interpretation is **falsified** by the matched component diagnostic. Preserve that result. Product qualification of the fail-closed compiler/validator remains useful; any new paper-level claim requires a genuinely fresh broader benchmark and competent contemporary direct/program-generation baseline. Do not tune the old controlled set.

## P1 — Darcy learned/OOD implementation readiness
`DARCY-FREEZE-001` is already frozen on canonical `main`; this is a protocol milestone, not an experiment result. Before any learned/OOD execution, retain implementation/environment hashes, exact split/data identifiers, matched model/compute budgets, metrics, uncertainty procedure and verifier. No result may be inferred from the protocol alone.

## P1 — APEN / Eigen-JEPA / NPMS
APEN and Eigen-JEPA remain preserved mixed/negative lines until separately frozen stronger controls and external/natural tasks justify new work. NPMS additionally requires original source/config/checkpoint recovery before any new science; preserve known negative spectral/switching/truncation cases.

## P2 — Hercules / Olympus
Archived from active compute for this cycle. Architecture names, parameter targets and governance/runtime demonstrations are not trained-model capability evidence. Reactivation requires a separately frozen decisive matched protocol.

## P1 — FinanceMeta / The Bu1LD target access
Canonical writable target/runtime surfaces and production authorization are not established in the current control surface. Do not substitute control-repository patches, local validations or public landing-page checks for production authorization/RLS/authenticated-role evidence.
