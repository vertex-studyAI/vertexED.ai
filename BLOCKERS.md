# BLOCKERS

**As of:** 2026-08-14 22:20 IST. Only blockers that prevent a stronger evidence claim are listed. Closed gates are not retained as blockers.

## P0 — Percy authoritative live state
Existing Mac SQLite/WAL/process/worktree state at `/Volumes/PRO-BLADE/Atlas/Percy` is not visible from this execution surface. Run `PERCY-STATE-001` non-destructively: snapshot/hash SQLite + WAL + checkpoint state, run integrity/schema checks, reconcile queue counters, leases, heartbeats and stale workers, record dirty worktrees, then independently recount. Do not reset, replace or reconstruct the DB to make counters clean.

## P0 — Project 2424 canonical source recovery
Selected child evidence remains preserved, but umbrella canonical local source/dirty-overlay recovery is unavailable. Recover verified HEAD/ancestry, overlay manifest + hashes, smallest authorized baseline verification and canonical child/disposition map before source-dependent new experiments. The 2,424 identity universe is not scientific completion.

## P0 — VertexED exact production revision + authenticated golden journey
Verified scheduled run `31817794439` passed bounded public/logged-out security/origin checks but live `/api/health` omitted expected deploy-relevant revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`; retained artifact ID `9225715176`, SHA-256 `e7870e9561748ef4d4247e3bf4e01d3e8feead3780c4e2016d3742d134f2069a`. Connected Vercel statuses reported deployment rate limiting. Required closure: identify canonical Vercel production deployment, restore authorized capacity without paid-upgrade workaround, prove body/header revision equality, make recurring monitor pass, then execute disposable-account authenticated core journey and cleanup.

## P0 — VertexED Supabase owner hardening
Connected project `xwlrzgfuhfbckgvcmyoq` is active. Read-only security advisor reports leaked-password protection disabled and security patches available for `supabase-postgres-17.4.1.074`. These are owner/platform hardening actions, not proof of an exploit or outage. Review Auth rollout and database upgrade/maintenance impact before mutation.

## P0 — LAM-JEPA owner-controlled release metadata + outside validation
LAM's negative scientific result and internal source/method/numeric/provenance/reviewer package are closed. Remaining gates are intentionally owner/external: approved license/redistribution boundary, author list/order, `CITATION.cff`, immutable release revision, and genuinely independent reproduction/skeptical review. Do not infer these from Git history or packet readiness.

## P0 product — FinanceMeta source review
Canonical repo `build-the-future-11/finance4all-global-reach` is recovered. Existing `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` is directly verified **41 commits ahead / 0 behind** `main@fbdd503223edc5b1780509720391083f485a4a85`. It already contains the retained hardening/release lineage, including migrations through `021_analytics_journey_events.sql`. Do not recreate those commits.

This connector can read that lineage, but a fresh branch-write attempt returned `403 Resource not accessible by integration`. Required closure: owner-authorized GitHub identity opens/reviews the existing branch against main and runs exact-head CI.

## P0 product — FinanceMeta live production qualification
Live FinanceMeta Supabase/deployment is not connected. Even after source review, production remains blocked until the complete intended migration/RLS state, environment, deployed revision, two-account cross-user denial/isolation, recovery/logout and saved-progress golden journey are verified on the real production target. Branch-local evidence is not live production evidence.

## P1 — IRIS frozen frontier source edge
Checksum-verified recovery is merged and substantially narrows the source gap. The complete visible `/Research/IRIS` Library contains only two recovered ZIPs; both were materialized and inventoried. No canonical frontier trajectory/NPZ/parquet artifact and no authoritative deterministic-equivalence declaration for `IRIS-FRONTIER-DEV-20260814` was found. Confirmatory seeds `1000–1029` remain untouched. Required closure: recover a private/local canonical raw trajectory artifact or pre-existing equivalence declaration plus frontier-specific immutable source/environment manifest. If absent, retain `PROTOCOL_BLOCKED`; do not regenerate ambiguously or execute the frontier.

## P1 — Darcy v2 remaining pre-outcome freeze
Current main already contains the frozen v2 protocol, deterministic generator/control scaffolding and machine-readable locks. Remaining prerequisites are exact eligible B2/B3/B4 learned implementations/configs, library/environment identity, hardware identity, model/parameter budgets, data/split/manifest hashes and protocol-conformity checks. No training/ID-test/OOD outcome may run until those prerequisites are immutable. Material changes require v3.

## P1 — NPMS canonical source identity
Recover original NPMS source/config/checkpoint before any new natural/OOD experiment. Deliver source identity + hashes + clean rerun against retained bounded evidence, or `SOURCE_UNRECOVERED`. Preserve negative spectral/switching/truncation cases; do not invent source.

## P1 — The Bu1LD target/data-plane access
Canonical writable repo/runtime/Supabase/deployment surfaces remain unavailable here. Current connected Supabase exposes VertexED only. Production authorization, RLS/object authorization, OAuth/env, deployment identity and seven-role journeys remain externally blocked.

## P1 — APEN / Eigen-JEPA / NeuroCAD / Hercules / Olympus
- APEN and Eigen-JEPA remain behind stronger matched/statistical/naturalistic controls; no in-place rescue.
- NeuroCAD typed-parser causal interpretation is already falsified; new science requires a fresh broader benchmark, not retuning the old diagnostic.
- Hercules/Olympus remain archived from active compute until decisive matched protocols exist.

## Scheduling guard

**Zero new major scientific experiment runs are authorized while these higher-information recovery/review/production/pre-outcome gates remain open.**
