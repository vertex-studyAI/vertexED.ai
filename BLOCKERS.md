# BLOCKERS

**As of:** 2026-08-14 after production monitor `31817794439` and Darcy v2 protocol freeze. Only blockers that prevent a stronger evidence claim are listed.

## P0 — Percy authoritative live state
Existing Mac SQLite/WAL/process state is not visible from this execution surface. Run `PERCY-STATE-001` non-destructively: snapshot/hash, integrity/schema, leases/heartbeats/stale reconciliation, independent recount. Do not reset or substitute GitHub branch/task counts for live Percy truth.

## P0 — Project 2424 canonical source recovery
Selected child evidence is retained, but umbrella canonical source/dirty overlay recovery depends on preserved local/Inkling state. Recover verified HEAD/ancestry and hash the dirty overlay before source-dependent new experiments. Registry count is not scientific completion.

## P0 — VertexED exact production revision
Production monitor `31817794439` failed three bounded attempts because `/api/health` was healthy but did not expose expected deploy-relevant revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`; homepage/API-router/malformed-waitlist/logged-out auth+admin/origin boundaries passed. Evidence artifact `production-health-31817794439` is artifact ID `9225715176`, SHA-256 `e7870e9561748ef4d4247e3bf4e01d3e8feead3780c4e2016d3742d134f2069a`. The expected SHA resolves in the repository. Direct Vercel project/deployment evidence is still required to determine why the served function exposes neither runtime nor generated revision provenance. Exact served revision/deployment ID plus authenticated disposable-account journey remain required.

## P0 — LAM-JEPA owner/release metadata
The frozen negative result, source/method reconciliation, deterministic paper assets, numeric provenance and immutable external-review packet are internally closed. Current LAM `main` is `bf8311e1a4d240e2891e51af38eaf7754944e300`. Remaining release blocker is owner-controlled metadata: approved author list/order, license/compatibility decision, `CITATION.cff`, redistribution boundary and immutable release revision. External validation remains pending until a genuinely independent report is returned.

## P1 — IRIS baseline frontier source
The development-only false-open/recovery/error frontier protocol is already frozen. Execution is blocked on recovery and hashing of the exact retained trajectories, implementation/parameters and metric code required by that protocol. Seeds `1000–1029` remain quarantined. If exact source cannot be recovered, record `PROTOCOL_BLOCKED`; do not reconstruct approximate evidence.

## P1 — Darcy v2 preflight
The learned/operator/OOD comparison is already frozen in `portfolio/project2424/projects/T2424-0050/LEARNED_OPERATOR_OOD_PROTOCOL_V2.md`. Execution is blocked until the exact implementation/environment, dataset/version+hashes, split manifest, matched budgets, metric/uncertainty code and falsifier implementation are committed and hashed. Do not change the frozen protocol after outcome inspection.

## P1 — NPMS source identity
Original NPMS source/config/checkpoint identity is not recovered. Preserve the bounded negative spectral/switching/truncation evidence; no new natural/OOD experiment is authorized until source identity is recovered or the line is explicitly classified `SOURCE_UNRECOVERED`.

## P1 — NeuroCAD new scientific claim
The old typed-parser causal interpretation is **falsified** by component v2 and is not a rescue target. Any new paper-level claim requires a genuinely fresh broader benchmark and competent contemporary direct/program-generation baseline. Do not tune the old 20 cases.

## P1 — APEN / Eigen-JEPA
Remain secondary until their predeclared dangerous learned/statistical controls and natural/OOD gates are complete. Existing mixed/negative evidence must not be upgraded by analogy.

## P1 — Hercules / Olympus
No significant compute until decisive matched protocols are frozen. Architecture names and scale targets are not capability evidence.

## P1 — FinanceMeta / The Bu1LD target access
Canonical writable target/runtime surfaces remain unavailable through the connected evidence surface; do not substitute control-repo overlays for target production evidence. Restore authorized repository/runtime/Supabase access before mutation and qualification.
