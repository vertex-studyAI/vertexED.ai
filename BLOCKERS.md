# BLOCKERS

**As of:** 2026-08-14 convergence re-verification. Only blockers preventing stronger evidence claims are listed.

## P0 — Percy authoritative live state — `BLOCKED_EXTERNAL_MAC`
Existing Mac SQLite/WAL/process state is not visible here. Checked-in `.percy/*` is historical provenance only. Snapshot/hash, integrity/schema, leases/heartbeats/stale reconciliation and independent recount are required. Do not reset or replace the DB.

## P0 — VertexED exact production revision — `BLOCKED_EXTERNAL_DEPLOYMENT_IDENTITY`
The public surface has returned healthy while omitting immutable revision identity; connected Vercel contexts also report deployment rate limiting. Exact served revision/deployment ID and an authenticated disposable-account golden journey remain required. Do not buy a paid upgrade or churn code merely to force deployment.

### VertexED platform-security human actions
Bounded Supabase inspection found RLS enabled on every inspected `public` table and no anon/authenticated EXECUTE privilege on the two inspected public `SECURITY DEFINER` functions. Security Advisor still warns leaked-password protection is disabled and Postgres security patches are available. Owner/admin review and approved maintenance remain required; database inspection does not close production certification.

## P0 — Project 2424 canonical source recovery — `BLOCKED_EXTERNAL_SOURCE`
Selected child evidence is retained, but umbrella canonical source/dirty overlay recovery depends on preserved local/Inkling state. Full 2,424-ID disposition cannot be fabricated from partial registries.

## P0 — FinanceMeta source/security recovery — `PARTIAL / MUTATION_BLOCKED`
Historical security merge `f18d0f008351a86accbc5ca2fa6ebbec05e57906` is genuine, but current `main` is the pre-merge parent `fbdd503223edc5b1780509720391083f485a4a85`. The preserved security branch remains 41 commits ahead. Latest exact-head CI fails lint; attempts to create a recovery branch/PR through the connected integration return 403. Do not force-move `main`.

## P0 — FinanceMeta production — `BLOCKED_EXTERNAL_RUNTIME`
Production Supabase/runtime and authenticated saved-progress journey are not accessible here. Source recovery is not production security certification.

## P0 — The Bu1LD production — `BLOCKED_DEPLOYMENT_VERIFICATION`
Repository access is no longer the blocker: current `main` is readable and exact-main CI passed. Cloudflare verification failed because `release:check` exited 1; deploy was skipped. Production remains unverified until exact-SHA deployment and required role journeys pass.

## P1 — LAM-JEPA external/owner closure
Internal reproducibility and immutable external packet are complete. Remaining gates: owner-controlled license/authorship/citation/redistribution metadata and genuinely independent outside reproduction/review.

## P1 — IRIS baseline frontier — `PARTIALLY_RECOVERED / PROTOCOL_BLOCKED`
Checksum-matched v0.2, addendum, common-harness and exact source-lineage archives were recovered. The remaining frozen-protocol gaps are exact canonical development trajectories and exact adaptation-metric freeze provenance. Confirmatory seeds `1000–1029` remain quarantined; do not regenerate substitutes.

## P1 — Darcy learned/OOD v2 — `FROZEN / PRE-OUTCOME MATERIALIZATION ONLY`
Protocol is already frozen and initial implementation/config materialization is on `main`. The remaining gate is exact implementation/environment/split/model-budget/dataset hash closure plus eligibility evidence. No learned training, ID-test or OOD outcome execution before every frozen prerequisite exists.

## P1 — APEN / Eigen-JEPA / NPMS
NPMS remains source-identity blocked. APEN/Eigen-JEPA remain secondary behind dangerous controls; frozen mixed/negative evidence must not be retuned in place.

## P1 — Hercules / Olympus
No significant compute until decisive matched protocols are frozen. Names/scale targets are not capability evidence.
