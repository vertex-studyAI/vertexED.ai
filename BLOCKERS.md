# BLOCKERS

**As of:** 2026-08-14 22:02 IST. Only blockers that prevent a stronger evidence claim are listed.

## P0 — Percy authoritative live state
The real Mac host `/Volumes/PRO-BLADE/Atlas/Percy` and its current SQLite/WAL/process state are not visible from this execution surface. Run `PERCY-STATE-001` non-destructively: snapshot/hash, **current free-space check**, SQLite integrity/schema, leases/heartbeats/stale reconciliation, independent task recount and dirty-worktree inventory. A prior real-Mac preflight observed internal free space below Percy's 25 GiB safety floor, but that is historical evidence and must be remeasured. Do not reset or replace the DB.

## P0 — VertexED exact production revision
Newest scheduled production monitor `31817794439` failed three bounded attempts because `/api/health` returned healthy but exposed **no revision**, while the deployment guard correctly expected deploy-relevant revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`. Homepage/API-router/malformed-waitlist/logged-out AI-user-admin/untrusted-origin smoke boundaries passed. Evidence artifact `9225715176`, SHA-256 `e7870e9561748ef4d4247e3bf4e01d3e8feead3780c4e2016d3742d134f2069a`; issue `#137` remains open.

Source already generates revision metadata during `prebuild`, and the health handler emits it when available. The remaining blocker is the **served build/deployment identity**, not source implementation. Vercel evidence also records the free daily deployment quota as exhausted. Do not spend money, create no-op deploy commits or weaken the health gate. When the existing quota window permits, make one production deployment from the latest verified runtime state, require `/api/health` revision equality and monitor PASS, then run the authenticated disposable-account golden journey.

## P0 — Project 2424 canonical source recovery
The retained workbook contract defines exactly 2,424 unique IDs/proposals, while selected canonical child packages exist in the control repository. The dirty umbrella source `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` remains unavailable here. Preserve/hash HEAD + staged/unstaged/untracked overlay, verify ancestry and export a named child inventory before treating the 2,424 universe as source-backed. Do not add overlapping historical 24/34/55 counts.

## P0 — LAM-JEPA release closure
The scientific result is reproducibly negative and the internal manuscript/reproduction packet is prepared. Remaining blockers are owner-controlled license/authorship/citation/redistribution/release revision plus genuine outside reproduction/review. Packet readiness is not external validation.

## P1 — IRIS baseline frontier
Current successor search is closed. A new architecture is not a blocker because it is not authorized. Exact retained raw trajectories/implementations/metric code required by the frozen development-only frontier protocol must be recovered; approximate regeneration is forbidden. Confirmatory seeds `1000–1029` remain quarantined.

## P1 — Darcy v2 implementation/evidence readiness
`DARCY-FREEZE-001` is **closed**: the learned-operator/OOD protocol v2 was frozen before outcome execution in commit `6fbd9c4ba73a460f5abbe6a6f4c478b6bc50e389`. The v2 result remains unexecuted. Before any run, verify that the generator, split manifest, learned baselines, environment, eligibility tests and evidence outputs exactly implement the frozen protocol; otherwise record the implementation gap rather than altering the protocol after seeing results.

## P1 — NeuroCAD new scientific claim
The old typed-parser causal interpretation remains falsified by component v2. Any new paper-level claim requires a genuinely fresh broader benchmark and competent contemporary direct/program-generation baseline. Do not tune the old 20 cases.

## P1 — APEN / Eigen-JEPA / NPMS
Remain secondary until dangerous learned/statistical controls and natural/OOD tasks are complete. NPMS specifically has recovered compact evidence in canonical Git, but the **original** scientific source/config/checkpoint migration and clean rerun remain pending.

## P1 — Hercules / Olympus
No significant compute until decisive matched protocols are frozen against canonical source/mechanism.

## P1 — FinanceMeta / The Bu1LD target access
Canonical writable target/runtime surfaces remain unavailable; do not substitute control-repository activity for production evidence.
