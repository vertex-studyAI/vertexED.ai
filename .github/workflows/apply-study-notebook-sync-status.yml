name: Apply Study Notebook sync status

on:
  pull_request:
    branches: [main, master]
    paths:
      - .github/workflows/apply-study-notebook-sync-status.yml
      - scripts/apply-study-notebook-sync-status.mjs
      - src/pages/StudyNotebook.tsx
      - tests/study-notebook-sync-status.test.mjs

permissions:
  contents: write

concurrency:
  group: study-notebook-sync-status
  cancel-in-progress: false

jobs:
  patch-test-and-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out branch
        uses: actions/checkout@v5
        with:
          ref: percy/study-notebook-sync-status
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply guarded patch
        run: node scripts/apply-study-notebook-sync-status.mjs

      - name: Run focused regression
        run: node --test tests/study-notebook-sync-status.test.mjs

      - name: Run canonical release checks
        run: npm run ci

      - name: Commit verified source change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/StudyNotebook.tsx tests/study-notebook-sync-status.test.mjs
          git diff --cached --exit-code && { echo "No source change produced"; exit 1; } || true
          git commit -m "Surface Study Notebook cloud-sync status"
          git push origin HEAD:percy/study-notebook-sync-status
