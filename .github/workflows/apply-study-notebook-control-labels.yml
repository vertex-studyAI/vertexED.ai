name: Study Notebook control labels

on:
  pull_request:
    branches: [main, master]
    paths:
      - src/pages/StudyNotebook.tsx
      - tests/study-notebook-control-labels.test.mjs
      - .github/workflows/apply-study-notebook-control-labels.yml
  push:
    branches: [main, master]
    paths:
      - src/pages/StudyNotebook.tsx
      - tests/study-notebook-control-labels.test.mjs
      - .github/workflows/apply-study-notebook-control-labels.yml

permissions:
  contents: read

concurrency:
  group: study-notebook-control-labels-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Study Notebook control-label regression
        run: node --test tests/study-notebook-control-labels.test.mjs
