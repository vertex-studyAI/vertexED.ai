# USER_ACTION_REQUIRED

**As of:** 2026-08-15 08:42 IST. Only owner/local/external actions remain here.

1. **Percy:** expose the preserved `/Volumes/PRO-BLADE/Atlas/Percy` on the Mac. This execution runtime has no `/Volumes` mount. Snapshot/hash SQLite+WAL+SHM+checkpoint, run integrity/schema checks, recover live counters/processes/leases/heartbeats/stale workers and dirty worktrees. Never initialize replacement state or migrate merely to make it green.
2. **Project 2424:** Wave-001 historical base is already recovered. Expose/hash the later `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` dirty overlay or byte-identical archive plus provenance/migration records explaining old `P2424-*` ↔ current `T2424-*` identities. Never remap by numeric suffix alone.
3. **VertexED deployment:** latest scheduled run `31860931665` still shows a healthy `/api/health` with the immutable revision missing, while expected deploy revision is `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`; artifact `9240538693`, SHA-256 `205b0d17ba3c1899addd558f2c0615ab32148af43a5f8fb6a55a510f4eb66394`. Source already contains and tests the immutable revision contract. Using existing authorized capacity only, identify/deploy the exact runtime revision, make the monitor pass, then run the disposable-account authenticated journey + isolation/recovery/logout/admin cleanup.
4. **VertexED platform security:** review/enable leaked-password protection and schedule the available hosted PostgreSQL security update with maintenance/rollback planning.
5. **LAM-JEPA:** approve author list/order, license/redistribution boundary, `CITATION.cff`, immutable release tag; send exact packet `218ea1bea686cdf8c281520b2b636897bc8b8dd2` to a genuinely independent validator. Outreach alone is not validation.
6. **FinanceMeta CI/source review:** preserve existing `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` (41 ahead / 0 behind recovered main). In `.github/workflows/ci.yml` blob `5df3a10c74ede1445f9008e99852278488ceeb91`, the E2E `env:` mapping repeats a second trio of `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL`. The minimal correction removes only that second trio; local YAML parsing validates the corrected structure. This connector cannot write the repo (`403 Resource not accessible by integration`), so apply the one-file fix through an owner-writable path, run exact-head audit/lint/typecheck/unit/build/release/Playwright gates, then review/merge on evidence. Do not recreate or squash the 41 commits.
7. **FinanceMeta production:** exact hardening SHA has successful Vercel Preview `5501026657`, explicitly not production. Connect the real production Supabase/deploy target and verify intended migrations/RLS/env/revision, role-escalation denial, multi-account isolation, core journey, recovery/logout + cleanup.
8. **The Bu1LD deployment:** public production smoke is directly verified: run `31857746101`, job `94945553076`, artifact `9239560598` (`sha256:6e1d265586f64f044b85150d7f1d6418557c422a56c231d0e5d09c1a951e4036`) records 8/8 HTTP 200 public routes. Do not infer immutable deployment identity or DB/Auth correctness. Required public names: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` or `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_EMAIL_ENDPOINT`. Required server-only names: `SUPABASE_DB_URL` or `SUPABASE_DB_PASSWORD`, `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `DIGEST_API_SECRET`, `CLOUDFLARE_API_TOKEN`; optional targeted purge uses `CLOUDFLARE_ZONE_ID` and `BU1LD_PURGE_CACHE=1`. Never print values. Configure these on the canonical repository/environment, rerun the existing exact deployment workflow, capture source SHA/deployment identity/served revision/production Supabase+Auth identity, then run seven-role journey and cross-role denial checks.
9. **IRIS:** the metric-freeze specification is recovered on canonical main at `portfolio/research/IRIS_SEQUENCE_ADAPTATION_METRIC_FREEZE_20260813.md` (blob `6f4d6a47e3727596b21714bc269cd8ba5844d2fa`). Recover the exact canonical development trajectory artifact or a pre-existing authoritative deterministic-equivalence record, plus the exact executable metric implementation needed to cross-hash against the specification. No approximate trajectory regeneration; seeds `1000–1029` stay forbidden; no frontier result run.
10. **Darcy v2:** `training_authorized=false`. B2 and split manifest are frozen; B3 FNO and B4 DeepONet remain `BLOCKED_IMPLEMENTATION`; covariance and OOD-D interpretation approvals, exact learned environment and hardware identity are unresolved. Close only these pre-outcome gates under the already-frozen parameter/compute budget. Do not train or inspect ID/OOD outcomes.
11. **NPMS successor only:** controlled source recovery is complete and current result is `PARAMETER_CONFOUNDED_OR_NON_UNIQUE`. Do not search for replacement source. If continuing, approve a new frozen natural/causal protocol before any run.
12. **PEN only if strategically retained separately:** recover standalone executable MODEL-PEN source; never inherit APEN evidence.

## No rescue action
Do not manually promote/rescue LAM-JEPA, NGMT v0.1, IRIS successor, Eigen-JEPA primary result, NeuroCAD typed-parser mechanism or T2424-1863. Hercules/Olympus require no immediate compute.
