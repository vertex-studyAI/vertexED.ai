# USER_ACTION_REQUIRED

Only actions that genuinely depend on access, ownership or decisions unavailable from the connected execution surface are listed. Agent-executable work does not belong here.

1. **Percy host access:** make the real Mac path `/Volumes/PRO-BLADE/Atlas/Percy` available without resetting or replacing its state. Before heavy work, remeasure internal free space because a prior real-Mac preflight fell below Percy's 25 GiB safety floor. Then permit non-destructive SQLite/WAL snapshot + integrity/recount/lease/worktree inspection.
2. **Project 2424 umbrella source access:** expose the preserved `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` source and dirty overlay so HEAD/ancestry/staged/unstaged/untracked hashes can be captured before reconciliation. Do not clean or reconstruct the overlay.
3. **VertexED production:** no paid Vercel upgrade is authorized. When the existing free deployment quota window permits, authorize/trigger exactly one meaningful production deployment from the latest verified runtime state if the platform does not do so automatically. Production cannot be promoted until `/api/health` serves the exact revision, the scheduled monitor passes and the authenticated disposable-account golden journey is completed.
4. **LAM-JEPA release metadata:** decide/approve license compatibility, author list/order, citation metadata, redistribution boundary and immutable release revision. Separately send the already-prepared immutable packet to a genuinely independent validator/reviewer if external validation is desired. Do not label outreach as validation before evidence returns.
5. **FinanceMeta / Bu1LD:** expose/authorize the canonical writable repositories and real runtime/Supabase targets before target mutations or production certification.
6. **PEN, Hercules, Olympus:** only if these lines remain strategically desired, expose/select one canonical standalone source/mechanism before a new versioned matched protocol is frozen. They are not tonight's P0 blockers and should not displace truth recovery/product certification.

No manual promotion or rescue is required for LAM-JEPA's frozen result, NGMT v0.1, the closed IRIS successor search, Eigen-JEPA's frozen mixed/negative primary result, NeuroCAD's falsified typed-parser mechanism or T2424-1863. Preserve those outcomes.
