# USER_ACTION_REQUIRED

Only actions that depend on access or decisions not available from the connected evidence surface are listed. These are **not** permission to weaken gates, recreate missing state, or invent evidence.

1. **Percy P0 — recover the real host at `/Volumes/PRO-BLADE/Atlas/Percy`.** Before creating or retrying work, preserve a checksummed copy of the canonical SQLite database, WAL and current checkpoints; run non-destructive integrity/schema checks; reconcile leases/heartbeats/stale workers; record physical worker/process state and real queue/task counters; record repository/worktree dirty state. If the host/source is unavailable, leave counters `UNKNOWN/BLOCKED` rather than initializing a replacement database.
2. **Project 2424 P0 — expose the preserved canonical source/dirty overlay.** Recover the actual local/Inkling source, verified HEAD/ancestry and dirty-overlay hashes needed by `P2424-CANON-001`. Do not reconstruct a missing canonical tree from the GitHub control overlay and do not add historical counts together without identity evidence.
3. **VertexED P0 — inspect the canonical Vercel project/deployment, then restore served-revision proof.** Latest monitor run `31817794439` expects deploy-relevant revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`, but live `/api/health` omits revision. Verify the production project and deployment ID; inspect whether Vercel system environment variables are exposed to the deployment and whether the deploy/build actually stamps or bundles `api/_generated/build-revision.js`; do not expose secret values in GitHub. Change configuration only when the canonical project is confirmed, create/allow a fresh production deployment if needed, then require the scheduled monitor to PASS. After revision identity is proven, complete issue #13's disposable-account authenticated journey and cleanup record.
4. **LAM-JEPA P0 — make owner-controlled release decisions.** Approve the author list/order, license and compatibility/redistribution boundary, then add `CITATION.cff` and choose an immutable release revision. These fields must not be inferred from commit history. Separately send the existing immutable external-validation packet at `218ea1bea686cdf8c281520b2b636897bc8b8dd2` to a genuinely independent validator; outreach is not validation until a returned report exists.
5. **FinanceMeta / Bu1LD — expose/authorize the canonical writable repositories and runtime/Supabase targets** before attempting target mutations, deployment claims, RLS certification or golden journeys. Control-repo overlays are not target production evidence.
6. **IRIS / NPMS source recovery — expose exact retained source artifacts if they exist outside the connected GitHub surface.** IRIS needs the exact retained trajectories/implementation/metric code required by its frozen frontier protocol; NPMS needs its original source/config/checkpoint. If exact source is unrecoverable, record the precise blocked verdict rather than recreating equivalent-looking evidence.
7. **PEN — recover the standalone executable MODEL-PEN source tree** only if a separate PEN research claim is still strategically desired; never inherit APEN evidence.
8. **Hercules / Olympus — no compute action now.** Reactivate only after one canonical mechanism and a bounded, matched-budget, predeclared falsifiable protocol exist. Parameter targets/provider names are not capability evidence.

No manual promotion or rescue is required for LAM-JEPA's frozen scientific result, NGMT v0.1, the current IRIS successor candidate, Eigen-JEPA's frozen mixed/negative evidence, T2424-1863, or the NeuroCAD typed-parser causal mechanism. Preserve those outcomes exactly.
