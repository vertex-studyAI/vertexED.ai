# USER_ACTION_REQUIRED

As of: **2026-08-13 22:00 IST**

Actions that require owner/device/account authority:

1. Make the canonical Percy Mac workspace and its existing SQLite/WAL available to the real 28-hour runtime so live counters can be measured rather than inferred.
2. Authorize the canonical FinanceMeta and The Bu1LD repository targets, plus any required live project connections, before target-mutation or production claims.
3. Restore or confirm VertexED deployment capacity, then verify the exact served revision and a safe authenticated golden journey.
4. Review the previously identified VertexED authentication/database maintenance warnings and re-run the relevant security checks after owner-approved maintenance.
5. Keep IRIS reserved confirmatory seeds closed until its candidate, baselines, primary metric, effect statistic, gate, falsifier, and analysis plan are frozen.
6. Keep the LAM-JEPA locked ARC test closed for the failed hypothesis line.
7. Provide/authorize external datasets or matched providers only after the corresponding NeuroCAD, Darcy, NGMT/APEN/Eigen/NPMS, Hercules, or Olympus protocol is frozen.

No owner action is required to preserve the negative LAM-JEPA evidence; that evidence should remain unchanged.