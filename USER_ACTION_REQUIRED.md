# USER_ACTION_REQUIRED

**As of:** 2026-08-14 22:01 IST. Only actions that genuinely depend on owner decisions, local-machine state or external access not available from the connected execution surface are listed.

1. **Percy / Project 2424 local state:** make `/Volumes/PRO-BLADE/Atlas/Percy` and the preserved Project 2424 canonical source/dirty overlay available to the execution surface. Before any reset or new source-dependent science, snapshot/hash SQLite + WAL + checkpoint state, run integrity/schema checks, reconcile leases/heartbeats/stale workers and dirty worktrees, and measure the currently `UNKNOWN` task/dispatch counters.

2. **VertexED deployment identity/capacity:** identify the canonical Vercel production project/deployment and restore authorized build/deploy capacity without weakening the revision contract. Latest verified public monitor run `31817794439` expects deploy-relevant revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a` but live `/api/health` omits revision identity; public/security boundary checks otherwise pass. After exact revision proof is restored, run the authenticated disposable-account golden journey and cleanup.

3. **VertexED Supabase security settings:** the live connected Supabase security advisor currently reports two warnings on project `xwlrzgfuhfbckgvcmyoq`: leaked-password protection is disabled, and database version `supabase-postgres-17.4.1.074` has security patches available. Enabling Auth leaked-password protection and scheduling a Postgres upgrade are owner/platform actions; review rollout/maintenance impact before applying them. Do not treat either warning as evidence of an exploit.

4. **LAM-JEPA owner-controlled release metadata:** decide/approve the root license and third-party compatibility boundary; approve the author list/order; approve `CITATION.cff` title/version/identifiers; decide dataset/code redistribution boundaries; choose an immutable release revision/tag. Repository history must not be used to infer these fields. The independent review/reproduction packet is already prepared, but external validation remains pending until a genuine outside report returns.

5. **FinanceMeta GitHub + Supabase access:** grant the connected GitHub integration contents/branch/PR write access for `build-the-future-11/finance4all-global-reach`. A fresh branch-creation test returned `403 Resource not accessible by integration`. Connect/authorize the actual FinanceMeta Supabase project as well; the current connector exposes VertexED only. Once both are available, the source-confirmed `profiles.role`/RLS/`SECURITY DEFINER` authorization defect can be patched on an isolated exact-base branch and verified with normal-member role-escalation denial tests before any production migration claim.

6. **The Bu1LD production access:** expose/authorize the canonical writable repository, real Supabase project and deployment surface. Then certify immutable deploy identity, hydration, RLS/role boundaries and the seven-role authenticated journey. The connected Supabase surface currently exposes VertexED only.

7. **IRIS / NPMS source recovery:** provide the exact retained IRIS trajectories/source/metric code and original NPMS source/config/checkpoint if they exist only in local/private storage. Approximate regeneration is not an acceptable substitute for these source-gated scientific lines.

8. **PEN:** recover the standalone executable MODEL-PEN source tree if a separate PEN research claim is still desired; do not inherit APEN evidence.

## No owner rescue required

No manual promotion or positive-result rescue is required for LAM-JEPA's frozen scientific hypothesis, NGMT v0.1, the current IRIS successor candidate, Eigen-JEPA's frozen primary result, NeuroCAD's typed-parser mechanism interpretation or T2424-1863. Their negative/mixed/falsified evidence must remain preserved.

Hercules and Olympus also require **no immediate compute action**. They remain archived from active compute until a genuinely decisive matched protocol is frozen; architecture/model names or scale targets alone do not justify reactivation.
