# USER_ACTION_REQUIRED

**As of:** 2026-08-15 08:42 IST. Only owner/local/external actions that cannot be completed from the connected execution surface remain here.

1. **Percy preserved host:** expose `/Volumes/PRO-BLADE/Atlas/Percy`. Before any reset/migration, snapshot and hash existing SQLite + WAL + SHM + latest durable checkpoint; then run integrity/schema and read-only process/task/worker/lease/heartbeat/stale/dirty-worktree inspection. Current live values are UNKNOWN.

2. **Project 2424 later overlay:** expose/hash `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` or a byte-identical archive plus provenance records linking historical `P2424-*` generations to current `T2424-*`. Wave-001 is already separately recovered. Never join generations by numeric suffix alone.

3. **VertexED exact production identity:** review PR `#397` (`8cf6c65d1786358e902952c5dee4e1250be9948b`). Exact-head CI `31861346546` is green, but do **not** auto-merge merely for that reason. If the source change is approved, merge/deploy the exact reviewed revision using existing authorized capacity; require the canonical production monitor to observe the exact immutable served revision; only then run the disposable-account authenticated signup/onboarding/core-persistence/isolation/recovery/logout/admin-denial journey + cleanup.

4. **VertexED platform security:** review/enable leaked-password protection and schedule the available hosted PostgreSQL security update with maintenance/rollback planning. These warnings do not establish an exploit.

5. **FinanceMeta source gate:** preserve `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f`. This integration cannot create the required isolated branch (`403`). On an owner-writable GitHub path branch from that exact head and remove **only** the second duplicate E2E mapping of `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL` from `.github/workflows/ci.yml`; do not change values or squash/recreate the 41 commits. Validate workflow/YAML and run audit, lint, typecheck, unit, build, release and Playwright gates before review/merge.

6. **FinanceMeta production:** after source review, connect the actual production Supabase/deployment target and prove intended migrations/RLS, exact served revision, member role-escalation denial, multi-account isolation, saved-progress/core journey, recovery/logout/admin denial and cleanup. Preview evidence is not production certification.

7. **The Bu1LD deployment:** verify/provide the repository secrets already expected by the existing workflow—`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`—without sharing their values in chat or committing them. This integration cannot list current secret presence (`403`). Then rerun the existing exact deployment workflow without rewriting wiring and retain workflow run, source SHA, deployment identity, served revision, public health and production DB/Auth identity. Full closure still requires the seven-role journey, cross-role denials, recovery/logout and cleanup.

8. **LAM-JEPA release metadata + external review:** review draft public packet PR `#86` (`dbd9117370f74c43f667974bf6431dd7cd1a760f`); approve license/redistribution boundary, author list/order, final citation metadata and immutable release tag/revision. Then send the immutable reproduction/review task to a genuinely independent validator. An invitation or author rerun is not external validation.

9. **Darcy v2 pre-outcome approvals:** before any training/outcome access, explicitly approve or require a new protocol version for (a) the current periodic/circulant finite-grid squared-exponential covariance interpretation and (b) OOD-D global offset `mu=0`. Separately record the exact permitted hardware/runtime environment once B3 FNO and B4 DeepONet implementations are frozen. Approval does not authorize training until every remaining freeze field is complete.

## No owner action required for current frozen result

- **IRIS source/metric provenance:** requested deterministic-equivalence/executable provenance gate is closed; seeds `1000–1029` remain forbidden and no frontier run is authorized.
- **NPMS:** current result remains `PARAMETER_CONFOUNDED_OR_NON_UNIQUE`; no rescue/source search.
- **NGMT v0.1, Eigen-JEPA primary, NeuroCAD typed-parser mechanism, T2424-1863:** preserve frozen negative/mixed/falsified results.
- **Hercules/Olympus:** no immediate compute action; remain archived from active compute until a decisive matched protocol exists.
