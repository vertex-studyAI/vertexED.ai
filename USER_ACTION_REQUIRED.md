# USER_ACTION_REQUIRED

**As of:** 2026-08-15 08:55 IST. Only owner/local/external actions that cannot be completed through the connected evidence surfaces remain here. Do not expose secret values.

1. **Percy — UNKNOWN:** on the actual Mac with `/Volumes/PRO-BLADE/Atlas/Percy`, first make a checksummed copy/snapshot of the existing SQLite DB plus WAL/SHM/checkpoints, then run integrity/schema checks and directly query task/queue/worker/lease/heartbeat/stale-worker counts, processes, dirty worktrees and latest durable checkpoint. Never initialize, migrate, reset, clean or replace state merely to make Percy work.
2. **Project 2424 — BLOCKED:** expose/hash `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` later dirty overlay plus direct provenance/migration records connecting historical `P2424-*` identities to current `T2424-*` identities. Keep historical Wave-001, later overlay and current source-directory layer separate. Never map generations by numeric suffix alone.
3. **VertexED deployment — BLOCKED:** source fix is merged at `d52308aed22ccc3dcefa7d4e3dd90aa731bc5f5a`, but both real deployment attempts failed. Inspect exact Vercel logs using the status-provided commands for `dpl_74bsey5Ht3PfDRFqJYcffxVKJTzV` and `dpl_44fbtnhTd7RFnA5QK5SauSHcKZKy`; repair only the evidenced failure, redeploy using existing authorized capacity, and prove `/api/health` serves the exact immutable revision. Then run the disposable-account authenticated journey, account isolation, recovery/logout, admin denial and cleanup. Do not weaken the identity gate or buy capacity.
4. **VertexED platform security — PARTIAL:** owner reviews/enables leaked-password protection and plans the available hosted PostgreSQL security update with maintenance/rollback discipline. These platform warnings are separate from deployment identity.
5. **FinanceMeta source — BLOCKED:** preserve `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f`. On an owner-writable path remove only the second duplicate E2E occurrences of `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL` in `.github/workflows/ci.yml`; do not change their values. Strict duplicate-key validation confirms this exact deletion repairs YAML structure. Then rerun exact-head audit, lint, typecheck, unit, build, release and Playwright gates and review/merge only if they execute/pass. This integration's branch write returned 403, so the 41-commit lineage was not recreated or squashed.
6. **FinanceMeta production — BLOCKED:** after source CI closes, connect the real production Supabase/deployment target and verify applied migrations/RLS, exact served revision, role-escalation denial, multi-account isolation, core persistence, recovery/logout/admin and cleanup. Existing Preview evidence is not production evidence.
7. **The Bu1LD deployment — BLOCKED:** verify repository Actions secrets named `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` exist without exposing their values. This integration cannot list secret metadata (403). Once present, rerun the existing `Deploy Cloudflare` workflow at canonical source `daa80c1124b2a6d7d09b7669e04d29e50cffcbbe`; retain run ID, source SHA, deployment identity and served revision. Then connect real Bu1LD Supabase/Auth, verify live phase33/RLS/functions/grants, and complete seven-role cross-role denials, recovery/logout and cleanup.
8. **LAM-JEPA release — PARTIAL:** the owner-controlled metadata handoff is now merged on `LAM-JEPA/main` as `55d716e8fcfdcd3e4c50af9bac0478b09994cedd`. Explicitly approve the still-placeholder license/compatibility, author names/order, release title/version/date and citation metadata; do not infer authors from Git history. Separately send immutable packet `218ea1bea686cdf8c281520b2b636897bc8b8dd2` to a genuinely independent validator. External validation remains BLOCKED until returned outside evidence exists.
9. **IRIS — BLOCKED:** recover exact canonical development trajectory artifact or a pre-existing authoritative deterministic-equivalence record, plus the exact executable metric implementation; cross-hash it against metric-freeze blob `6f4d6a47e3727596b21714bc269cd8ba5844d2fa`. Do not regenerate approximate trajectories; seeds `1000–1029` remain forbidden; no frontier result run.
10. **Darcy v2 — PARTIAL:** before any training, close B3 FNO and B4 DeepONet implementation/eligibility, exact learned environment/dependency lock, hardware identity, periodic/circulant covariance interpretation approval and OOD-D global-offset interpretation approval. Existing split hash/budgets remain frozen; no outcome inspection is authorized.

## No owner rescue action
Do not retune or manually promote LAM-JEPA, NGMT v0.1, Eigen-JEPA primary, NeuroCAD typed-parser mechanism, T2424-1863 or the current NPMS result. No new major scientific outcome run is authorized.
