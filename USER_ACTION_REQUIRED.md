# USER_ACTION_REQUIRED

**As of:** 2026-08-15 09:04 IST. Only actions requiring owner/local/external access or decisions are listed.

1. **Percy — UNKNOWN:** mount the preserved `/Volumes/PRO-BLADE/Atlas/Percy` on the Mac. Snapshot/hash SQLite+WAL+SHM+latest durable checkpoint before any mutation; run integrity/schema; recover direct task/queue/worker/lease/heartbeat/process counts, stale workers and dirty worktrees. Never initialize replacement state or migrate merely to make it green.
2. **Project 2424 — BLOCKED:** expose/hash the later dirty `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` overlay or a byte-identical archive plus explicit identity-migration records. Historical Wave-001 is already recovered; never infer `P2424-*` ↔ `T2424-*` identity by numeric suffix.
3. **VertexED production — BLOCKED:** source closure is `d52308aed22ccc3dcefa7d4e3dd90aa731bc5f5a` with CI `31861346546`. Push monitor `31861568506` still observes served behavior inconsistent with that source; artifact `9240733558`, SHA-256 `2c74c4c71bf2f1e03ebe2144ae9c499e13b53292d0a020c1003c1887c0ed18ef`. Using existing authorized Vercel access/capacity only, identify the actual serving project/deployment/runtime environment, prove the served artifact contains the fail-closed source and exact revision, rerun the main monitor, then run the disposable-account authenticated journey + isolation/recovery/logout/admin cleanup.
4. **FinanceMeta — BLOCKED:** preserve `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f`. Through an owner-writable path, edit only `.github/workflows/ci.yml` to remove the second duplicated `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL` trio from `test:e2e`; then run exact-head audit/lint/typecheck/unit/build/release/Playwright gates. The connected GitHub integration returns 403 for writes, so no fix is claimed applied here.
5. **The Bu1LD deployment — BLOCKED:** configure the four exact names observed in the existing workflow—`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`—without exposing values. Rerun the existing workflow at `daa80c1124b2a6d7d09b7669e04d29e50cffcbbe`; capture workflow run, source SHA, Cloudflare deployment identity, served revision and production DB/Auth identity. Then execute the seven-role journey and cross-role denials.
6. **IRIS — BLOCKED:** executable metric provenance is recovered. Supply/recover the exact canonical development-trajectory artifact or pre-existing authoritative deterministic-equivalence evidence. Do not regenerate approximate trajectories, use seeds `1000–1029`, or run the frontier.
7. **LAM-JEPA — PARTIAL:** metadata boundary is merged as `cf988f3275a25419995df60ade5931bc0270f9c0`. Approve license/redistribution, author list/order, `CITATION.cff` and immutable release revision/tag; then send the immutable packet to a genuinely independent validator. Outreach or internal reruns do not count as validation.
8. **Darcy T2424-0050 v2 — BLOCKED:** keep `training_authorized=false`. Approve covariance interpretation and OOD-D global-offset interpretation; establish exact learned environment/dependency lock and hardware identity; complete eligible B3 FNO and B4 DeepONet implementations under the frozen budget. Do not train or inspect ID/OOD outcomes.
9. **FinanceMeta production — BLOCKED:** after corrected exact-head CI, connect the exact production deployment/Supabase target and verify migrations/RLS/revision, role escalation denial, multi-account isolation, core journey, recovery/logout/admin and cleanup.
10. **VertexED platform security — PARTIAL:** review leaked-password protection and the available hosted PostgreSQL security update through the platform with maintenance/rollback planning; do not infer production certification from source policy evidence.

## No rescue action

Do not retune or manually promote LAM-JEPA, NGMT v0.1, Eigen-JEPA primary, the NeuroCAD typed-parser mechanism, T2424-1863, current NPMS or the current IRIS successor state. No new project creation is required.
