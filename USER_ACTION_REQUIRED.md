# USER_ACTION_REQUIRED

1. Mount/restore the real Percy host at `/Volumes/PRO-BLADE/Atlas/Percy` before deep execution; run SQLite integrity/WAL/lease/heartbeat/provider/RAM checks and reconcile the canonical DB against this closeout before dispatch.
2. VertexED production: repair/redeploy exact revision identity so `/api/health` exposes the served SHA, then repeat the production golden journey.
3. FinanceMeta / Bu1LD: provide canonical writable repository/runtime/Supabase access before target mutations.
4. PEN: recover standalone executable MODEL-PEN source tree if a separate PEN claim is still desired.
5. Hercules: identify one canonical source path and bounded proposed mechanism before matched Transformer compute.
6. Olympus O1: choose an eligible frozen provider only when all four arms can share the same provider/tool/task budget.

No user action is required to rescue LAM-JEPA, NGMT v0.1, IRIS’s current successor or T2424-1863. Preserve their negative results.
