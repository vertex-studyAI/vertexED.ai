# USER_ACTION_REQUIRED

**As of:** 2026-08-14 convergence run. Only actions requiring owner decisions, local-machine state or external access are listed.

1. **Percy + Project 2424:** expose preserved `/Volumes/PRO-BLADE` state. Snapshot/hash SQLite+WAL+checkpoint; integrity/schema; counters/leases/heartbeats/stale workers/dirty worktrees. Recover Project 2424 source/ancestry/overlay separately. Never initialize replacements.
2. **VertexED deployment:** identify canonical production deployment, restore exact served-revision proof with existing authorized capacity, make monitor PASS, then run disposable-account authenticated golden journey + cleanup.
3. **VertexED platform security:** review/enable leaked-password protection and schedule the available hosted PostgreSQL security update with rollback/maintenance planning.
4. **LAM-JEPA release:** approve author list/order, license/redistribution boundary, `CITATION.cff`, immutable release revision/tag; send exact packet `218ea1bea686cdf8c281520b2b636897bc8b8dd2` to a genuinely independent validator. Returned evidence, not outreach, advances validation.
5. **FinanceMeta CI/source review:** preserve existing `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` (41 commits ahead / 0 behind recovered main); do not recreate it. Exact-head Actions run `29641469740` failed with zero jobs because `.github/workflows/ci.yml` has duplicate `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and `VITE_APP_URL` keys in the E2E `env:` map. Use an owner-writable GitHub path to remove the duplicate keys on the same release-candidate lineage, rerun exact-head CI until jobs execute/pass, then review/merge on evidence. The exact SHA has a successful Vercel **Preview** deployment only; GitHub marks `production_environment: false`.
6. **FinanceMeta production:** connect/authorize the real FinanceMeta Supabase/deployment target after source review; verify intended live migration/RLS state, exact production revision, member role-escalation denial, multi-account isolation, core saved-progress journey, recovery/logout and cleanup. Preview/source evidence is not production certification.
7. **The Bu1LD release environment:** source `ryangomez010/bu1ld-landing@daa80c1124b2a6d7d09b7669e04d29e50cffcbbe` passes exact-main CI (`29679123068`). Cloudflare run `29679123047` failed before deployment because `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` were empty; the workflow already maps `${{ secrets.VITE_SUPABASE_URL }}` and `${{ secrets.VITE_SUPABASE_ANON_KEY }}` correctly. Add/verify the intended repository Actions secrets—never place values in source—then rerun the exact SHA. If verification passes, prove served revision and seven-role authenticated journeys.
8. **IRIS provenance:** expose the exact canonical development trajectories + frozen adaptation-metric source identity. No approximate regeneration; confirmatory seeds `1000–1029` remain forbidden.
9. **Darcy pre-outcome review:** before any training, explicitly accept the current periodic/circulant finite-grid squared-exponential covariance interpretation and the current OOD-D global log-offset choice (`mu=0`), or version a changed protocol before outcomes. This approval does not authorize training; B2/B3/B4/env/hardware/budget/manifest gates still remain.
10. **NPMS successor only:** controlled Atlas source recovery is complete and the current regime-classification headline is `PARAMETER_CONFOUNDED_OR_NON_UNIQUE` under the frozen adverse control. Do not search for a replacement controlled source. If NPMS continues, approve a **new frozen natural/causal protocol** with invariant-parameter and state-space/spectral controls before any new run.
11. **PEN only if retained separately:** recover standalone executable MODEL-PEN source; never inherit APEN evidence.

## No rescue action
Do not manually promote or rescue LAM-JEPA, NGMT v0.1, current IRIS successor, Eigen-JEPA primary result, NeuroCAD typed-parser mechanism or T2424-1863. Their negative/mixed/falsified evidence remains preserved. Hercules/Olympus require no immediate compute.
