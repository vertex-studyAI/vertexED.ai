# NEXT TASK QUEUE

**Rescored:** 2026-08-15 08:42 IST.  
**Rule:** information gain × closure probability × evidence value ÷ cost. Dependencies are hard. **Zero new major scientific experiment runs are authorized.**

## 1 — PERCY-STATE-001 — P0 / BLOCKED_EXTERNAL_MAC
This execution runtime has no `/Volumes` mount. Recover on the preserved Mac only: current free space; snapshot/hash SQLite+WAL+SHM/checkpoint; integrity/schema; independent counters; processes; leases/heartbeats/stale workers; dirty worktrees. Never reset, migrate merely to make it green, or initialize replacement state.

## 2 — P2424-CANON-002 — P0 / HISTORICAL BASE + CURRENT IDENTITY GUARD VERIFIED; LATER OVERLAY/MIGRATION BLOCKED
Historical Wave-001 is checksum-recovered: bundle SHA-256 `4c685af70d84052c026602ff7336a522c741d91fb480038e980c21f0bbc63ece`, head `ff609f335f91297357b430a2531633fe111cd5a9`, 2,424 registry rows but only **24 source-backed packages**, 0 independent reproductions, release rejected, fresh clean-clone quality gate passed. Current repository source identity is guarded by `portfolio/project2424/SOURCE_IDENTITY_MANIFEST.json` + CI for 23 represented T2424 directories against the frozen First-100 queue.

**Next gate:** expose/hash the later dirty `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` overlay or byte-identical archive and recover cross-generation identity-migration provenance, then build a collision-aware child map. Historical `P2424-*` and current `T2424-*` assignments demonstrably collide; never join by numeric suffix or add historical counts.

## 3 — VERTEX-PROD-001 — P0 PRODUCT / BLOCKED_EXTERNAL_DEPLOYMENT_IDENTITY
Latest scheduled monitor `31860931665` on control `main@8d8c0bf215c59668519b2ed7ef7fad35f185f53a` failed after three bounded attempts because live `/api/health` remained healthy while omitting immutable revision identity, so it could not match expected deploy revision `e2ecd19ed9816f8f36369c7dc0f38e39942ca73a`. Homepage, unknown-route 404, malformed waitlist 400, logged-out ask/user-content/admin 401 and untrusted-origin 403 checks passed. Artifact `9240538693`, SHA-256 `205b0d17ba3c1899addd558f2c0615ab32148af43a5f8fb6a55a510f4eb66394`.

The source-side immutable revision contract is already implemented and unit-tested; do not duplicate it.

**Next gate:** existing authorized capacity only. Identify/deploy the exact runtime revision, prove `/api/health` body/header revision equality and monitor PASS, then disposable-account auth/onboarding/persistence/isolation/recovery/logout/admin cleanup. Do not buy capacity or weaken the revision assertion.

## 4 — FINANCEMETA-REVIEW-001 — P0 PRODUCT / SOURCE RECOVERED, MINIMAL CI FIX IDENTIFIED, WRITE BLOCKED
`build-the-future-11/finance4all-global-reach`; existing `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` is 41 ahead / 0 behind `main@fbdd503223edc5b1780509720391083f485a4a85`. Direct workflow inspection at blob `5df3a10c74ede1445f9008e99852278488ceeb91` confirms the E2E `env:` block repeats exactly the second trio `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL`. A correction removing only that second trio parses successfully in local YAML validation. GitHub connector file/branch/PR writes to this repository return `403 Resource not accessible by integration`; therefore the fix is **not applied** and no fresh corrected exact-head CI is claimed.

**Next gate:** owner-writable path preserves the existing 41-commit branch, removes only the duplicate second env-key trio, then audit/lint/typecheck/unit/build/release/Playwright jobs execute and pass on the exact corrected head before review/merge.

## 5 — FINANCEMETA-PROD-002 — P0 PRODUCT / BLOCKED_EXTERNAL_LIVE_STATE
After source/CI closure, connect the real target and verify applied intended migrations/RLS, authorized env, exact served revision, role escalation denial, multi-account isolation, saved-progress journey, recovery/logout/admin and cleanup. Preview is not production proof.

## 6 — BU1LD-PROD-001 — P0 PRODUCT / PUBLIC ROUTES VERIFIED; EXACT IDENTITY + DATABASE/AUTH BLOCKED
Canonical source remains `ryangomez010/bu1ld-landing@daa80c1124b2a6d7d09b7669e04d29e50cffcbbe`; exact-head CI `29679123068` succeeded; source apply/verify reaches phase33. Scheduled `Bu1LD Production Health` run `31857746101`, job `94945553076`, completed **success**. Artifact `9239560598`, SHA-256 `6e1d265586f64f044b85150d7f1d6418557c422a56c231d0e5d09c1a951e4036`, records 8/8 HTTP 200 checks at `https://thebu1ld.com` for `/`, `/signup`, `/login`, `/projects`, `/programs-public`, `/evidence`, `/privacy`, `/terms`.

Required secret/environment names are already documented; values must never enter source. Public: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` or `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_EMAIL_ENDPOINT`. Server-only: `SUPABASE_DB_URL` or `SUPABASE_DB_PASSWORD`, `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `DIGEST_API_SECRET`, `CLOUDFLARE_API_TOKEN`; optional targeted purge `CLOUDFLARE_ZONE_ID` + `BU1LD_PURGE_CACHE=1`.

**Next gate:** owner supplies the required names on the canonical deployment repository/environment, reruns the existing exact deployment workflow and captures workflow run, source SHA, Cloudflare deployment identity, served revision and production Supabase/Auth identity. Then verify seven-role journey and cross-role denials. Do not rewrite working deployment wiring.

## 7 — LAM-RELEASE-METADATA-003 — P0 / BLOCKED_OWNER
Owner-approved license/redistribution, author list/order, `CITATION.cff`, immutable release revision/tag. Do not infer owner-controlled fields.

## 8 — EXTVAL-LAM-001 — P1 / READY_EXTERNAL_PACKET
Send immutable packet `218ea1bea686cdf8c281520b2b636897bc8b8dd2` to a genuinely independent validator for reproduction + skeptical review. External validation remains pending until returned evidence exists.

## 9 — IRIS-FRONTIER-SOURCE-001 — P1 / METRIC FREEZE RECOVERED, TRAJECTORY/EQUIVALENCE PROVENANCE BLOCKED
Checksum-backed retained source lineage is recovered. Current main directly contains the metric freeze at `portfolio/research/IRIS_SEQUENCE_ADAPTATION_METRIC_FREEZE_20260813.md`, blob `6f4d6a47e3727596b21714bc269cd8ba5844d2fa`: `TWMSE25` uses `W=25`; recovery requires the first five consecutive samples within `0.10*D`; `POST_MSE50PLUS` begins at `t0+50`.

Exact canonical development trajectory arrays remain unrecovered, and byte/equivalence provenance between the executable metric code and the frozen specification remains open. Confirmatory seeds `1000–1029` remain forbidden and no frontier run is authorized.

**Next gate:** recover/materialize the authoritative trajectory artifact or a pre-existing authoritative deterministic-equivalence record, then cross-hash the exact executable metric implementation against the recovered specification. Until those edges close, retain `PROTOCOL_BLOCKED`.

## 10 — DARCY-V2-MATERIALIZE-003 — P1 / B2 + SPLIT FROZEN, REMAINING PRE-OUTCOME BLOCKERS
Training remains unauthorized. `v2-freeze-config.json` records `training_authorized=false`; B2 PCA+ridge is `IMPLEMENTED_PREOUTCOME_UNIT_VERIFIED`, blob `6e10c6fbecf0cf5ce78ed2b5c61e3fa97da47541`; split-manifest SHA-256 `4211d11da7d40f0991bd963c04fb118f34d9fe923e7664da301122b29b0bef85` is frozen. B3 FNO and B4 DeepONet remain `BLOCKED_IMPLEMENTATION`. Covariance interpretation approval and OOD-D global-offset approval are null; exact learned environment and hardware identity are null.

**Next gate:** close only B3/B4 eligibility implementations, exact dependency/environment lock, hardware identity and the two pre-outcome interpretation decisions within the already-frozen compute/parameter budget. No learned training or ID/OOD outcome access until every blocker is immutable and independently checked.

## 11 — NPMS-SUCCESSOR-FREEZE-002 — P2 / SOURCE RECOVERED, RESULT REPRODUCED, MECHANISM NON-UNIQUE
Controlled result remains `PARAMETER_CONFOUNDED_OR_NON_UNIQUE`: NPMS `92.86%` vs invariant-parameter `89.29%`, gap `3.57pp` inside the predeclared `5pp` band. No natural/OOD run is authorized.

## 12 — JEPA-TS-FREEZE-001 — P2 / DORMANT
No run authorized; only a separately frozen cheap synthetic falsification gate may reactivate this programme after higher-value gates close.

## 13 — PORTFOLIO-RESCORE-003 — P1 / WAITING_DECISIVE_EVIDENCE
Re-score only after material new evidence. Current Tier S count remains 3; do not promote work to fill a quota.

## Closed or materially narrowed this wave
Project 2424 historical Wave-001 recovery; current T2424 source identity invariant; NPMS controlled-source recovery + adverse-control verdict; Darcy B2/split freeze; IRIS metric-freeze source identity plus partial source recovery; Bu1LD source/CI recovery plus 8/8 public-route availability verification; FinanceMeta hardening source recovery plus exact minimal workflow defect identification; LAM internal verification/external packet packaging; NeuroCAD mechanism falsification.

None of those closures imply publication, external validation, trained Darcy v2 capability, immutable Bu1LD/VertexED deployment identity, authenticated production behavior, real-user validation or portfolio-wide completion.
