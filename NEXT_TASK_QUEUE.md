# NEXT_TASK_QUEUE

**Rescored:** 2026-08-16 13:01 IST  
**Rule:** information gain × closure probability × evidence value ÷ cost. Dependencies are hard. Zero new major scientific outcome runs are authorized.

## 1 — PERCY-STATE-001 — P0 — UNKNOWN
Recover the preserved `/Volumes/PRO-BLADE/Atlas/Percy` state on the Mac only. Snapshot/hash SQLite+WAL+SHM+latest durable checkpoint before touching anything; run integrity/schema; query directly available task/queue/worker/lease/heartbeat counts; inspect processes, stale workers and dirty worktrees. Never initialize replacement state or migrate merely to make it work. This connected runtime still cannot see the Mac `/Volumes` surface. Connected Drive searches for Percy checkpoints/source routed to the registry rather than canonical runtime bytes, and the registry itself still marks Percy as `UNRESOLVED_REMOTE` with local discovery required. The Library recovery surface also returned `401 Unauthorized`, so no absence claim is inferred from that source. No live Percy counters are inferred.

## 2 — P2424-CANON-002 — P0 — BLOCKED
Historical Wave-001 is already recovered and must stay separate. Expose/hash the later dirty `/Volumes/PRO-BLADE/Atlas/Project-2024/Project_2424` overlay or a byte-identical archive plus explicit provenance/migration records connecting historical `P2424-*` identities to current `T2424-*` identities. Never join by numeric suffix alone. Drive searches for `Project_2424`, `P2424`, `Wave-001` and `T2424 migration` did not surface a canonical source/archive beyond the project registry, whose identity map still marks Project 2424 as local-discovery/`UNRESOLVED_REMOTE`; Library search is currently connector-blocked with `401 Unauthorized`. No substitute overlay was synthesized.

## 3 — VERTEX-PROD-001 — P0 — BLOCKED
Source-side fail-closed production identity remains VERIFIED, while fresh production monitor `31929731079` still observed `/api/health` with revision missing across all three bounded attempts. Public/API security smoke checks passed. The retained monitor artifact downloaded in this sweep hashes to `c3fc1fb59ee3da3c791420f1567873cdc566791f98f8985cea22bf232d775bb4`. Connected Supabase read-only inspection confirms all current public tables have RLS enabled and owner-bound profile/artifact policies are present, while leaked-password protection and a managed Postgres security update remain external dashboard/infrastructure actions. Recent OAuth traffic reaches Supabase; some new-account callbacks are rejected with `422 Signups not allowed for this instance`, so authenticated certification still requires the intended approved disposable identity. Inspect the actual Vercel production project/runtime environment, prove exact immutable served revision, rerun the monitor, then complete approved-account onboarding/core persistence/isolation/recovery/logout/admin cleanup.

## 4 — FINANCEMETA-CI-001 — P0 — BLOCKED
Preserve `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` (41 ahead / 0 behind). The only isolated workflow syntax repair remains removal of the second duplicated `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_APP_URL` trio from `test:e2e`. A fresh minimal Contents-API write attempt in this sweep was again rejected with `403 Resource not accessible by integration`. Use an owner-writable path; then run exact-head audit, lint, typecheck, unit, build, release and Playwright gates before review/merge.

## 5 — BU1LD-PROD-001 — P0 — BLOCKED
Exact source `daa80c1124b2a6d7d09b7669e04d29e50cffcbbe` remains code-CI healthy. Cloudflare deploy run #13 fails in `release:check` before deployment because intended Supabase deployment variables are empty; deploy is skipped. Configure the four observed workflow secret names—`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`—without printing values, rerun the existing workflow, capture deployment/source/served revision/DB/Auth identity, then run the seven-role journey and cross-role denials. Old Cursor feature branches are fully contained in main and hold no stranded commits.

## 6 — IRIS-FRONTIER-SOURCE-001 — P1 — BLOCKED
The stale control-tree visibility blocker for the standalone adaptation-metric freeze has been closed on draft PR #406: current main contains `IRIS_SEQUENCE_ADAPTATION_METRIC_FREEZE_20260813.md` at Git blob `6f4d6a47e3727596b21714bc269cd8ba5844d2fa`, and exact-head CI #1304 / run `31932568823` passed on `237418db8d00c283d6430c165182ccc7ee11d658`. Drive searches for `IRIS trajectory`, `IRIS_FRONTIER`, and the retained source-archive hash prefix did not surface a canonical trajectory artifact; the Drive registry itself marks IRIS as `UNRESOLVED_REMOTE`/local-discovery and says not to regenerate missing canonical trajectories. Library search is connector-blocked with `401 Unauthorized`, so no absence claim is made from that surface. The remaining execution blocker is canonical raw development-trajectory provenance (or pre-existing authoritative deterministic-equivalence evidence). Seeds `1000–1029` remain forbidden. No frontier result run.

## 7 — LAM-RELEASE-001 — P1 — PARTIAL
The negative-result public packet has been replayed from current main on draft PR #89 at `52bd96ca5bb9c1b0ce6aca0172ce4088ca61a2e8`; Research claim boundary #84, ARC Protocol V2 QA #165 and Reproducibility CI #206 all passed. The negative result, locked-test boundary and absence of external validation remain unchanged. Owner approves license/redistribution, author list/order, final citation metadata and release revision; then send the immutable packet to a genuinely independent validator.

## 8 — DARCY-V2-PREFLIGHT-001 — P1 — BLOCKED
Keep `training_authorized=false`. B2 PCA+ridge and the split manifest are pinned, while B3 FNO, B4 DeepONet, exact learned dependency/environment lock, hardware identity, covariance interpretation approval and OOD-D global-offset interpretation approval remain unresolved. Draft PR #407 adds an outcome-free fail-closed readiness preflight and tests at `3e13f555f188ad50843325021bd5f07760f23225`; exact-head CI #1305 / run `31932753671` **passed**. A branch sweep found no hidden B3/B4 implementation to recover. Do not train or inspect ID/OOD outcomes until every pre-outcome gate is explicitly closed in a reviewed freeze revision.

## 9 — WEATHER-JEPA-PREOUTCOME-001 — P1 — BLOCKED
Draft PR #404 latest head `8da37d1628162885fbda054eb25772a5d1e1e626` now includes the fail-closed multi-epoch iterator fix, B0 persistence baseline plumbing, synthetic unit coverage, a dedicated CPU-only Python smoke workflow, `PREOUTCOME_FREEZE.json`, and a machine-readable scientific-training preflight. A regression explicitly prevents a zero variance-regularizer weight from satisfying the declared collapse-guard gate. `Weather-JEPA smoke` #7 / run `31933827651` **passed** and repository-wide CI #1324 / run `31933827588` **passed** on this exact head. Scientific outcome execution remains unauthorized (`scientific_outcome_training_authorized=false`) until normalized-L2 reduction/axis/epsilon, variance-regularizer form/positive weight, block-mask rules, mask-ratio selection, B1/B2/B3, data/splits, model budget, environment/hardware, run plan, uncertainty and retention are explicitly frozen rather than guessed in implementation.

## 10 — NEUROCAD-PRODUCT-001 — P1 — PARTIAL
Current T2424-0037 supports a deterministic controlled-language → validated representation → OpenSCAD/SVG engineering pipeline with retained controlled, held-out-template, executable-CAD and component-ablation evidence. The latest frozen component diagnostic remains `VALIDATION_DOMINANT`: direct extraction plus matched fail-closed validation recovered the full B0→M2 gap, so the typed-parser/IR causal advantage is not supported on those reused diagnostic cases. Draft PR #409 reconciles this current truth and passed generic CI #1312, held-out workflow #15 and component-ablation workflow #5 at exact head `c57f00eb24ce2507ef8a381e7dd8aa3ce9af9c93`. Separate engineering draft PR #410 at `074d268ca37bd65b8cf59af6678a0655a6a54f48` adds strict validation for caller-constructed CAD specs before OpenSCAD/SVG/summary generation and passed generic CI #1313 / run `31933250986`, held-out #16 / `31933250961`, and component-ablation #6 / `31933250966`. Continue product hardening only; any new scientific claim requires a separately frozen broader part-family/compositional benchmark rather than tuning the old diagnostic.

## 11 — FINANCEMETA-PROD-001 — P1 — BLOCKED
After exact-head source/CI closure, connect the real production target and verify applied migrations/RLS, exact served revision, role-escalation denial, multi-account isolation, core journey, recovery/logout/admin and cleanup. Do not start production certification from the still-invalid hardening branch workflow.

## 12 — TEXT-TO-VIDEO-PROD-BOUNDARY-001 — P2 — PARTIAL
The local durable queue + content-addressed artifact-store boundary is integrated on main. Draft PR #32 reconciles the stale artifact-store status record at `3dd93b8c5470d3f2988e7ec96968973d85d3a38b`; exact-head CI #73 / run `31932198400` passed. Current evidence supports local single-host persistence/integrity only. Production object storage, authenticated ownership, distributed worker orchestration, retention/cleanup, observability and hosted media delivery remain future product work; do not relabel the local boundary as production.

## 13 — OBSCURED-VALIDATION-001 — P2 — PARTIAL
Current T2424-0038 Obscured Records Agent is no longer merely `VERIFYING`: implementation PR #178 merged from exact head `abf8c998bab4bc0adedfb3d1d1a19432603c355f`, and canonical CI #643 / run `31411209123` passed. Draft PR #409 reconciles the stale status to `TESTED_TOOL / MERGED`. Draft PR #411 at exact head `7a9bf7b087b3731ade2cfa68899ab599d75ef252` now freezes the next blinded historical-lead evaluation, privacy/de-identification boundary, B0/B1/B2 baselines, safety/usefulness gates, error analysis, a machine preflight, pure evaluator and synthetic regressions; full repository CI #1318 / run `31933552285` **passed**. `evaluation_authorized=false` remains explicit: corpus/label manifests, editor-blinding provenance, publisher-independence review, environment and execution hashes are still required before any real evaluation. Do not confuse current T2424-0038 with conflicting historical P2424-0038 identities by numeric suffix.

## 14 — HERCULES-SOURCE-001 — P2 — BLOCKED
`HERCULES_MATURITY.md` correctly keeps Hercules below H0 until canonical source is exposed. Installed-repository search found no Hercules repository, direct `vertex-studyAI/Hercules` lookup returns not found, and there is no Hercules branch in the control repository. Drive searches for `Hercules` surfaced only the master registry; no canonical implementation source was exposed. Library search is connector-blocked with `401 Unauthorized`. Do not invent a second implementation. Recover/expose the canonical source first; then run the frozen ≤10M-parameter H0 plumbing/overfit/checkpoint/determinism gate there.

## 15 — OLYMPUS-SOURCE-001 — P2 — BLOCKED
Historical O0 evidence for recovered deterministic Olympus runtime mechanics remains retained, but the connected GitHub/Drive surfaces do not expose the canonical recovered runtime source/archive (`olympus.pyz` search returned no artifact; no dedicated installed Olympus repository was found), and Library search is connector-blocked with `401 Unauthorized`. Draft PR #409 adds `OLYMPUS_SOURCE_STATUS_20260816.md` and passed generic CI #1312, held-out #15 and component-ablation #5 on exact head. Do not rebuild a replacement Olympus merely to run O1. Before the preregistered matched-provider O1 experiment, recover the canonical source and freeze immutable source identity, provider/model/version, task manifest, shared tool budgets, environment, randomization, per-task retention, latency/token/cost contract and resource boundary.

## Next-tier research controls completed in this wave

- **NPMS:** draft PR #412 creates `NPMS-BEYOND-PARAMETERS-V1`, preserving the `PARAMETER_CONFOUNDED_OR_NON_UNIQUE` adverse-control verdict while machine-blocking any successor evaluation until source/data/splits, mechanism target, parameter/state-space/spectral controls, primary/secondary metrics, uncertainty, compute and exact script identities are frozen.
- **Eigen-JEPA:** the same draft PR #412 creates `EIGEN-JEPA-MULTIDATASET-V1`, preserving `REPRODUCED_REAL_DATA_MIXED_NEGATIVE`, keeping persistence/raw-ridge/log-ridge/Cholesky-ridge plus the four-metric panel mandatory, forbidding fake deterministic seed replication, and blocking the multi-dataset replication until all pre-outcome identities and guardrails are frozen.
- **NGMT:** current v0.1 remains a reproducible negative/inconclusive research result; no successor run was launched merely to create a positive outcome.
- **PEN:** Drive searches did not surface the canonical source/executable, while Library search is connector-blocked with `401 Unauthorized`; the existing independent evidence package is not promoted to source-reproducible status.

## Completed this rescore

`PORTFOLIO-RESCORE-001` is satisfied by this evidence-only refresh because every top-level project in the current master registry has now been examined and is represented by either verified work, a bounded next gate, or an explicit source/access/owner blocker. This queue also records the additional NPMS/Eigen/NGMT/PEN sweep. The latest queue head must pass a fresh exact-head CI after this update. This rescore creates no replacement project, deployment claim, research-success claim or new major scientific outcome authorization.
