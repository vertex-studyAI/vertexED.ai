# Requested UI effects: implementation map

13 September 2026. Local React adaptations, not installed Vue components or claims of source parity. The current landing is `src/pages/Home.tsx`. Effects respect reduced motion and do not change learner records.

| Requested INC | Actual location / implementation |
| --- | --- |
| Lens | `/#concept-lens`, now magnified by default, with pointer and keyboard slider |
| Marquee | Late-page fictional testimonial design previews, expressly not endorsements |
| Media text | Beyond-the-paper heading, image-clipped existing Apex artwork with blue/white fill |
| Morphing tabs | Learning-stage selector with a moving active surface |
| Parallax float | Large revision card, bounded passive scroll depth and pointer tilt |
| SVG mask | Lens clips a genuinely scaled copy of the cooling curve |
| Timeline | Five-stage illustrative learner journey, with explicit step selection |
| Tracing beam | Native-scroll chapter trace and study-stage relationship lines |
| Blur reveal | Finite heading entrance, never required for readable content |
| Box reveal | Blue heading wipe, landing and Features |
| Container text flip | Revision answer changes in a stable container with a finite transition |
| Encrypted text | Topic metadata and study-gallery introduction, finite decode |
| Fluid cursor | Existing bounded CPU dye simulation on landing and Features |
| Image-trail cursor | Gallery heading uses up to four existing Apex images, clearing after 800ms |
| Tailed cursor | Same contained gallery interaction draws the recent pointer path; native cursor remains |
| Apple-style card carousel | `/#study-examples`, native horizontal scrolling plus arrows and expandable practice dialogs |
| Floating card | Gallery, revision sheet and existing subject cards; focus flattens them |
| Direction-aware hover | Gallery surface enters from the pointer's nearest edge |
| Ripple button | Primary landing button press feedback |
| Interactive hover button | Directional arrow and colour feedback on existing actions |
| Shimmer button | Finite sweep on primary calls to action and navbar invitation |
| Tetris | Automatic contained Revision Stack, manual takeover and local-only score |
| Neural background | Gallery heading's small software-rendered, pointer-responsive neural-style field; not OGL/GLSL |
| Interactive grid with Tetris | Only the playable board, including cell hover feedback; no decorative page-wide grid |
| Glowing effect | Pointer-lit study surfaces, cobalt boundaries and trace |
| Progressive blur | Existing section-island gradient blur and glass fallbacks, not blurred study text |

The unmarked Float, Link Preview, Scroll Island, animated-beam, neon and dither suggestions inform local depth, annotations, navigation, connecting lines and restrained texture. These are not separate installed library implementations. No scroll hijacking, effects switch, generated user claims or unrestricted model-authored executable UI.

## Real integrations are separate release gates

- Live Supabase project `xwlrzgfuhfbckgvcmyoq` is healthy. Its public settings endpoint returned HTTP 200 with Google and email enabled and general signup disabled.
- The current local environment has public authentication configuration but neither server service-role nor secret-key configuration. A signed-in non-admin cannot complete the server beta-access lookup without that credential.
- Hosted migration history does not match the local migration ledger, including newer school, account and learner-state changes. It also contains unrelated historical migrations. Do not run an unreviewed whole-directory push against this database.
- A read-only `information_schema.columns` check confirms the live `waitlist` lacks `application_profile`, `profile_collected_at` and `school_id`, and the `schools` table is absent. Existing account-link and status columns are present. No student records were queried and no live schema was changed.
- A real approved-account browser sign-in, hosted callback allowlist, schema reconciliation, production deployment and live AI-provider acceptance remain unverified. Mocked tests are not evidence for these gates.
- Imported guide audit remains 245 files, zero editorial approvals and 53 review flags. No fabricated approval or grade prediction has been introduced.
