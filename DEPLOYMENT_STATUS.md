# DEPLOYMENT_STATUS

**As of:** 2026-08-18 live production evidence refresh  
**Rule:** source/build evidence, database-control evidence, preview deployment, and exact production certification are separate states.

| System | Source/build evidence | Deployment/runtime evidence | Authoritative state | Next gate |
|---|---|---|---|---|
| VertexED | `main@e71da4a8341a2c236f6252b74d078e7464b681ab` makes the canonical CI build use the fail-closed deploy lifecycle; `api/_handlers/health.js` requires immutable revision identity in production. Live Supabase read-only audit on 2026-08-17 found RLS enabled on all 26 public tables inspected. Ownership-bearing tables use `auth.uid()`-bound policies; public testimonials are limited to `is_published = true`; no public views exist. The two `SECURITY DEFINER` functions in `public` (`auth_email_exists`, `handle_new_user`) have execute ACLs limited to `postgres`/`service_role`, not `anon` or `authenticated`. This narrows but does not replace the required multi-account end-to-end authorization test. | latest scheduled production-health run `32057805890` checked `https://www.vertexed.app` three times and independently reproduced the same single fail-closed condition: `/api/health` returns `ok` but omits the immutable revision expected to equal `e71da4a8341a2c236f6252b74d078e7464b681ab`. Root, unknown-route, malformed-waitlist, logged-out AI/user/admin, and untrusted-origin probes all passed. Evidence artifact `9296998668` has SHA-256 `fbec821ba80fcb58d9786f808b8f80db7fa1b46baeed1eb7ae6f5deda47417fc`; incident #137 was updated successfully in this run. Both Vercel status contexts for this main SHA previously failed: `vertex-ed-ai` deployment `dpl_GkCr7NkrA6JW2KiN9ZJ1z6sAN3zz` and `vertex-ai` deployment `dpl_6ZDPzzrVYFB9hrRopAwaLYzLHkhJ`. The `vertex-ai` integration has also recorded deployment failures on Aug 10, Aug 12, and Aug 15, so the current failure is persistent across multiple revisions rather than unique to the latest commit. Supabase security advisors still warn that leaked-password protection is disabled and that the hosted Postgres build has security patches available. | **BLOCKED — PERSISTENT VERCEL DEPLOYMENT FAILURE; SERVED REVISION + AUTHENTICATED JOURNEY UNVERIFIED** | inspect authenticated Vercel logs for both failed deployment IDs and account/project state, repair the deployment root cause without weakening immutable-revision checks, redeploy the intended SHA, rerun production-health to PASS, enable/resolve remaining Supabase security settings where owner-authorized, then run disposable-account signup/auth/onboarding/core persistence/isolation/recovery/logout/admin-boundary certification |
| FinanceMeta | `build-the-future-11/finance4all-global-reach`; recovered hardening branch `cursor/membership-security-supabase-fix@6dcc03710bb6adf9b4b722b308c40a0720bea61f` is 41 commits ahead / 0 behind recovered main; branch/content/PR writes through the current GitHub integration return `403 Resource not accessible by integration`; the branch CI workflow contains duplicate `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and `VITE_APP_URL` keys in one YAML `env` mapping, an exact workflow-admission defect that remains unlanded because of the write boundary | live production Supabase target and exact production revision are unavailable through current connector; source/preview evidence does not prove production RLS/auth isolation | **BLOCKED_EXTERNAL — OWNER WRITE PATH + PRODUCTION TARGET REQUIRED** | owner-authorized removal of the duplicate YAML env-key trio on the existing hardening branch, exact-head CI/security review, then live migrations/RLS/env/exact revision and two-account authorization/isolation/recovery/logout cleanup journey |
| The Bu1LD | canonical source `ryangomez010/bu1ld-landing@daa80c1124b2a6d7d09b7669e04d29e50cffcbbe`; exact-main CI run `29679123068` passed | historical Cloudflare deployment run `29679123047` failed before deploy because `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` were empty. However, current public production at `https://thebu1ld.com` is directly reachable: scheduled read-only smoke run `32058330450` passed 8/8 meaningful-HTML routes (`/`, `/signup`, `/login`, `/projects`, `/programs-public`, `/evidence`, `/privacy`, `/terms`). Evidence artifact `9297172702` has SHA-256 `cbb3a5f70a4b6e660c6e71b3acb659bf61706a1d3cbeddacfc73a13807351089`. This proves public-route availability only; it does not prove the served immutable revision, client-side execution, Supabase/auth configuration, or role-separated behavior. | **PARTIAL PRODUCTION EVIDENCE — PUBLIC ROUTES HEALTHY; REVISION + AUTHENTICATED ROLE JOURNEY UNVERIFIED** | preserve the 8/8 public-route proof, determine the actual production deployment/source identity behind `thebu1ld.com`, then certify exact served revision and run signup/login/onboarding/application/discovery/submission/review/approval/admin/cross-role-denial/logout journeys with cleanup; do not infer authenticated health from HTTP 200 public routes |
| Percy Prime | historical/control artifacts exist | live Mac SQLite/WAL/checkpoint/process/worktree state is unavailable in this execution surface | **BLOCKED_EXTERNAL_MAC** | non-destructive DB/WAL/checkpoint snapshot, integrity/schema, live counters/leases/heartbeats/stale-worker/worktree reconciliation before qualification |

## Product certification law

A CI pass, source branch, database metadata query, preview deployment, provider status, or HTTP 200 is **not** exact production certification.

Production status requires direct evidence for the intended immutable revision plus the relevant authenticated end-to-end journey. External rate limits, missing secrets, or inaccessible production targets remain blockers; they are not reasons to weaken gates, fabricate deployment state, or incur unapproved spending.
